Please create the service harness for our two-zone air sampler. Each zone has one fan and one pressure transmitter. I need removable zone connectors, clearly different labels, and sensor returns kept separate from fan returns until the controller star point. Use the attached schedule and route drawing, check simultaneous fan startup and voltage drop through every connector and shared segment, and give the assembler a complete pin schedule, cut list, BOM and illustrated instructions.
