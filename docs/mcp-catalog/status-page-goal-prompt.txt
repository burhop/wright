/goal Implement Wright's combined integration status page and repeatable QA/marketing data pipeline using the complete specification at D:\repos\wright\.local-run\mcp-curation\wright\docs\mcp-catalog\status-page-implementation-plan.md. Read it before coding; it is the acceptance contract.

Work only in D:\repos\wright\.local-run\mcp-curation\wright on codex/mcp-curation-lifecycle. Reuse the existing catalog, status generator and independent static dashboard. Follow AGENTS.md. Preserve unrelated changes and raw evidence. Commit local checkpoints; do not push, merge, deploy, contact vendors, create accounts, change live marketing, or add QA to Wright's application/API/package. This task implements the status system, not another broad MCP testing campaign.

Build ONE combined portfolio for MCP, WebMCP and hardware integrations. Baseline: 78 tracked records = 73 MCP + 3 WebMCP + 2 hardware. Protocol is a filter, not a separate default view. Use one clickable row: Works 10, Preview 4, Requires login 10, In progress 36, Abandoned 18, Blocked by vendor 0. These are baseline mappings, not hardcoded outcomes. Every canonical ID appears once; all six counts must reconcile to the current catalog. Explain evidence-driven corrections.

Green means successful checks within the disclosed scope; separate Works, Preview and Requires login. Yellow means planned/testing/repair/host or evidence work. Red means a reviewed abandoned decision. Give vendor restrictions their own neutral tile; require explicit vendor evidence, never infer prohibition from 401/403/404, licensing or ordinary login. Preserve the difference between fully qualified support and partial/authentication-only evidence; audit legacy green claims.

Every tile, including zero, opens one informative searchable list showing its members and why: purpose, protocol, scope, completed/unrun tests, versions/platforms, dates/freshness, login/host requirements, limitations, next step and evidence. QA also shows owner, priority, due date and diagnostic links. Remove duplicate metrics. Use bright yellow and distinct crimson with text labels. Support mobile, keyboard, shareable filters and browser back/reload.

Generate versioned, schema-validated current status and immutable dated history from one classifier. Supply separate QA and PUBLIC ALLOWLIST exports with identical counts. Public artifacts omit secrets, private correspondence, local paths and raw logs; hiding fields in HTML is insufficient. Keep stable repository page/JSON paths usable by future marketing without a new API/database.

Graph green totals and fully qualified totals over time from real observations, with accessible table and component counts. Preserve provenance, declines, corrections, policy/cohort changes, and idempotence. Never invent history or call technical coverage customer adoption. One real point is acceptable.

Implement a documented validate/build/record-snapshot/verify workflow, atomic last-good output preservation, and validation/artifact steps in existing CI. Scheduling may prepare reviewed candidates; no automatic promotion, abandonment or publication. Preserve existing process evidence as a secondary QA section; its absence must not break the status page.

Complete the plan's numbered phases and acceptance checks. Tests must verify unique membership, evidence and vendor rules, privacy, history, current/catalog consistency and every drill-down; remove fixed-24 assertions. Verify served QA and public builds with Playwright and screenshots. Leave http://127.0.0.1:18765/index.html running. Finish with counts, explained corrections, artifacts, schemas, exact update command, checks, commits and remaining limitations. Do not stop after a cosmetic mockup or claim completion with required work remaining.
