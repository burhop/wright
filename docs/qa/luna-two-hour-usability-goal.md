/goal Spend at least 120 minutes of substantive engineering work fixing Wright workflow usability and verifying complete user journeys. Start with the confirmed defects in docs/qa/luna-usability-review-2026-09-18.md. Implement and verify fixes; do not finish with a plan or a passing test count alone.

MODEL AND COST
Use the Luna model already selected for this task. Do not switch models, delegate, or spawn agents. If the session explicitly identifies a different model, report that mismatch before implementation; do not claim that this prompt changes the selected model. Keep reads and output focused. Use local deterministic component/API tests for most iterations and Playwright for complete user journeys. Do not invoke paid model calls, live engineering MCP operations, CAD/CFD/solver execution, printers, vendor services, or external publication. Do not install engineering servers, bypass qualification gates, commit, push, merge, or activate deferred output-content validation.

TIME AND PERSISTENCE
Record start time, earliest completion time (start plus 120 minutes), and a short activity ledger in docs/qa/luna-two-hour-usability-progress.md. Work through the entire queue autonomously. Elapsed time alone is insufficient: do not pad with sleeps, clock polling, repeated unchanged tests, or repetitive documentation. Check the clock only at substantive milestones. Exclude paused or idle time from active-time claims. Continue beyond two hours to finish an in-progress fix and verification. Stop early only for an explicit user stop or a genuine inability to do any remaining independent work; report it as incomplete. Do not mark the goal complete while acceptance criteria remain unverified or mandatory work is merely parked.

BASELINE
Read AGENTS.md, specs/081-engineering-workflow-templates/plan.md, docs/contributing/workflow-ui-integration.md, and the new review. Inspect the existing dirty diff before editing. Preserve user changes and existing workflows. Verify the checkout, frontend URL, API target and feature flags actually being served. Use disposable workspace data and isolated services for mutations. Never replace the reviewed workflow editor to simplify implementation.
Use the repository's locked dependency setup. Do not copy another checkout's node_modules. For Python, inspect the workspace/UV configuration and use a task-local writable cache and isolated environment where supported; request tool approval if required. Do not substitute compilation for API tests or repeatedly try the same failed setup. Keep environment changes separate from product changes.

WORK PACKAGE 1: CORRECT EXECUTION IDENTITY
Reproduce the false missing-binding warning using createMcpServerBlock and the actual inspector flow. Treat server-scoped AI MCP tasks (mcp-task) and exact MCP calls (mcp-tool) as different contracts. A server-scoped task requires the selected server and eligible tools; an exact call requires its exact saved server/tool. Preserve human review, supported AI tasks and internal operations without inventing MCP requirements.
Show useful saved execution identity on each block: server name for server-scoped work, server/tool for exact calls, resolved tool for binding references, and appropriate human/internal labels. Keep technical IDs available for diagnostics. Never change binding semantics based on a display name.
Verify unavailable saved selections stay visible and editable. Verify binding changes survive save/reopen and undo/redo. Add tests using real authoring factories, not fixtures that assume every MCP task has mcp_tool.

WORK PACKAGE 2: READINESS THAT STAYS CORRECT
Prevent stale or out-of-order responses from changing readiness for a different workspace, session, path or source revision. Cover delayed manual refresh, switching documents, save completion and multiple overlapping checks. Show checking, failed-check and unchecked states honestly; do not style a pending check as healthy.
Refresh MCP inventory after relevant configuration changes and provide an explicit recheck. Preserve unsaved work when navigating to setup controls and returning. Distinguish inventory membership from host health and qualification. Show the affected task, concrete next action and accountable role supported by evidence.
Inspect the existing runtime preparation and readiness services. Add a bounded, non-executing preflight contract for checks the host can actually perform safely. Reuse the same rules in the run gate to avoid diverging UI policy. Bind results to the saved subject and record check time and reason. Return unchecked when host health cannot be assessed without starting an engineering operation. Do not claim a service works because a callback or catalog entry exists. Enforce the authoritative run checks on the backend even when the UI has a stale result.

WORK PACKAGE 3: TRUTHFUL START, REJECTION AND RECOVERY
Separate a requested start from accepted execution and actual step/tool dispatch. A rejected start must say Run blocked, name the issue and action, and never say Prompt sent or imply a tool ran. Show saved/prepared input as such until dispatch evidence arrives. Audit errors against actual backend codes; replace substring guesses that misassign ownership. Preserve specific backend corrections and unknown codes without inventing their cause.
Test rapid Run and Save & run submissions while save and preflight are deliberately delayed. Fix any reproducible duplicate dispatch with a guard that covers the whole asynchronous start operation and reliably releases on every outcome. Keep prior diagnostics and partial outputs available after failure or cancellation without attributing them to a new rejected request. Verify an already-running job remains observed when reopening its workflow.

WORK PACKAGE 4: REAL CONTRACT AND JOURNEY COVERAGE
Fix the API test's incorrect detail.code assertion against the application's actual error envelope. Run focused endpoint tests for hand-authored sources, missing files, template readiness, malformed provenance, wrong scope, unavailable checks and qualification rejection. Confirm no generation/tool dispatch occurs during checks or rejection using fail-on-call spies.
Inventory existing Playwright files and test names; remove claims about nonexistent tests. Add the missing end-to-end binding and failure journeys through the normal workspace Workflow/Workflows control. Exercise blank New workflow, creation, instructions, valid and mismatched connections, source/layout save, conflicts, reopen, delete while typing, undo/redo, refresh and blocked-run correction.
Use mocked services for deterministic failure branches, and separately exercise a real local API with disposable files and stubbed execution dependencies. Mark the evidence type explicitly. No real engineering execution is authorized.
Inspect screenshots of the changed screens at desktop and narrow viewports. Verify readable binding names, compact readiness controls, reachable recovery actions and canvas space. Save and open the screenshots; retain them in a stable QA artifact directory rather than relying on trace files that another test run overwrites.

INDEPENDENT BACKLOG IF THE PRIMARY FIXES FINISH EARLY
Work on these in order while respecting the two-hour minimum:
1. Enumerate all packaged workflow templates using existing parsers/services. Produce a per-template readiness/binding/first-blocker matrix without executing workflows. Distinguish reference templates from configured but unqualified templates.
2. Verify keyboard-only configuration, focus restoration, 200% zoom and long server/tool names, fixing concrete usability defects found.
3. Investigate the reproducible one-worker Vitest onUserConsoleLog teardown error with bounded targeted experiments. The two-worker passing run is a workaround, not proof of the cause. Stop after two failed repair attempts for that root cause and record the reproduction, evidence and next action.
4. Improve a deterministic regression entry point so another agent can repeat the focused suite using existing test files and explicit service requirements. Fail on missing required fixtures or test targets.
5. Audit consistency among editor readiness, backend preflight and run errors across supported block types; add regression cases for concrete disagreements.

WORKING RULES
Make small, scoped changes and test the changed behavior. Reuse established models and helpers. Do not add tests that only assert an implementation's wording or mirror its assumptions. After focused checks pass, broaden once for integration risks; repeat only after a change, failure or a new diagnostic hypothesis. Do not spend the session rerunning green suites.
Keep a concise progress update after each meaningful improvement, and maintain the checklist across context transitions. If one root cause remains blocked after two evidence-backed repair attempts, record reproduction, diagnosis, owner and next action and continue independent work. Parking a mandatory acceptance item does not make it complete.

COMPLETION EVIDENCE
Before finishing, audit every work package against code and retained evidence. Report active work duration separately from wall-clock duration and explain any material pauses. List production fixes, the exact passing/failed/blocked checks, inspected screenshot paths, tested served URL and API target, and remaining issues with owners. Distinguish mocked journeys, real API tests, and real engineering execution (not performed). Correct the prior progress record's overclaims where evidence warrants it. No user-accepted or engineering-qualified claim without corresponding evidence. Leave a clear restart command and service lifecycle state. Never end merely because 120 minutes elapsed.
