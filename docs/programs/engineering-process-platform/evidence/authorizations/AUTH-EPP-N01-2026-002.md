# Prospective overnight development authorization

Record identity: `AUTH-EPP-N01-2026-002`. Program: `EPP-2026`. Feature: `EPP-N01` / `NATIVE-01`.

The user accepted the coordinator's preceding overnight proposal in this conversation with the exact words:

> Approved—continue overnight

The coordinator recorded this observation at `2026-09-05T02:59:17Z`. That is the observation time, not an asserted message-receipt timestamp. This Markdown record attributes the instruction to the conversation, retains its literal short acceptance, and summarizes the accepted proposal below. It does not claim a signature, approval of unseen exact bytes, a completed test, an independent review or human study evidence.

## Accepted scope

1. Continue the remaining independent review, required gates, feature-branch dev PR, terminal CI inspection, dev integration and verification of the integrated build. Preserve required quality checks and report concrete blockers while continuing independent work around external failures.
2. Publish **development images** to the project's existing GHCR and Docker Hub destinations using existing credentials. Bind build, publication and integrated verification evidence to the actual source and image identities. This is prospective authorization; it records no publication or deployment outcome.
3. Run `npm audit`, including transmission of dependency metadata to the configured npm registry. Classify the actual results and address required findings; permission to run the audit is not a passing audit result.
4. Track actual human usability separately as [FOLLOWUP-NATIVE-HUMAN-01](../followups/FOLLOWUP-NATIVE-HUMAN-01.md). Distinguish completion of the authorized technical delivery from actual human acceptance. Preserve the study protocol, previous history and pending human evidence.
5. Keep the existing standalone implementation dashboard at `http://127.0.0.1:8765/` current with implementation, verified quality, dev integration, actual build verification and remaining follow-up work.
6. Continue useful independent work around external failures and report the exact unresolved requirement. Required technical checks, independent review, security/compatibility findings and terminal CI remain substantive delivery conditions.

Production integration into `main`, production releases, purchases, new credentials and destructive actions remain excluded. Direct pushes to `dev` remain unsupported. This instruction does not authorize broader publication, paid services, proprietary installation, physical actuation or removal of historical data.

## Relationship to existing authority and completion records

The [original standing goal](AUTH-EPP-N01-2026-001.json) remains unchanged as the historical instruction. This later instruction adds the narrowly described development-image publication and npm audit permissions and separates human-study scheduling from overnight technical delivery. It does not rewrite the earlier authorization or imply that previously excluded actions occurred with authority.

The [existing scoped implementation delivery rule](../../coordinator-state-machine.md#prospective-native-implementation-delivery-revision-98-onward) already permits a reviewed dev implementation PR with human study pending. No policy or frozen contract change is required to proceed through dev integration. The fixed 32-task registry, exact reviewed candidate, task meanings, integration requirements and acceptance checks remain intact. T028/Q-HUMAN stay pending and explicitly linked to the separate follow-up; no agent result becomes a human result.

Technical delivery may be reported complete only after the actual authorized implementation, required reviews/checks/CI, dev PR integration and integrated-build journeys have succeeded. Whole-milestone human acceptance remains open until the follow-up supplies its actual evidence. Existing terminal program-state conditions retain their current meanings: record real deployment evidence separately without forcing a whole-milestone completion state while human obligations remain pending.

At this record's preparation, source `7c0d3ef3358b62493ac12e1bd823f901983e44f0` retains reviewed implementation candidate `60ef8672f1f61c2f4942e618638ec8901e9aa9a0` in `PUSH_AUTHORIZATION_PENDING`, state revision 101. The published milestone has 26/32 implemented, 26/32 verified and 0/30 integrated, with two integration exemptions. Final metadata review, dev PR/CI, actual integration and deployed-build verification are pending; this authority record grants none of those outcomes.
