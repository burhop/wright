# Historical local CodeQL analysis: d734f4c0

This bundle preserves the actual local Python CodeQL run on `d734f4c0955f1cce93855fab751cb3f64628dd79`, tree `0ad525b039858809a78e1fae1f8ae039d155a8aa`. Database creation and analysis exited 0. Its 43 reported rules matched H1 exactly, and the five original dashboard/native-route findings were absent. Two findings already present in H1 remained at unchanged source locations; they are retained without dismissal.

**This source was subsequently withdrawn after independent review found three issues in the shared scan helper.** The scanner result is historical evidence of what that run observed. It does not close those review findings, validate the corrected helper, clear GitHub CI, or grant whole-candidate, task, gate, integration or deployment completion. The corrected source requires its own actual analysis and all applicable checks.

`codeql-d734-public-projection.json.txt` contains only explicit source, tool, query population, result locations, timing and raw-evidence hash fields. It records the official CodeQL 2.26.4 bundle, exact matching Python pack versions, 3,475 verified source blobs, 1,226 processed modules, and five critical files explicitly extracted, including the shared scan helper. Creation completed at `2026-09-05T09:04:07.290306Z`; analysis completed at `2026-09-05T09:05:30.041286Z`. Full SARIF, logs and source manifests remain privately retained and hash-bound.

`host-preparation-history.json.txt` preserves the three earlier failed preparations separately: archive newline conversion, missing Windows launcher interpreter registration, and rejection of an absolute interpreter path by the declared extractor option. The successful fourth attempt used command-local Git settings preserving exact committed bytes and the supported `python` interpreter name on the subprocess PATH. No query, policy, global host configuration or repository source was changed.

The full raw SARIF, log bodies, absolute host paths, environment, configuration dumps, source snippets and traces are not copied into this public projection. SHA-256 values bind the exact retained raw originals or generated public bytes as labeled; they do not stand in for Git object identities. `public-file-manifest.json.txt` binds the three projected files and producer. Prepared by `/root/dashboard_review` from retained observations, without rerunning the historical analysis or publishing this bundle.
