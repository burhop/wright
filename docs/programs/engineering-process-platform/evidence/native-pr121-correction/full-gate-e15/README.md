# Completed e15 full local gate — public receipt

The actual `scripts/check-dev-merge.sh` run exited **0**, from **2026-09-05 09:18:33.3468613Z** to **09:51:29.1779068Z**. It tested frozen source `e15c07497e0440d243ea61b1b7e2e4d0a9f1bd25`, tree `b7dad8c29c9ae447a1cf60750ff4bf1512a0706b`, for product candidate `28efd6cc`. This is a receipt for that full command, with its warnings and skips retained.

Frontend unit tests: **547 passed**. Browser: **170 passed, 5 skipped, 0 unexpected, 0 flaky, no retries**. Independent review verified all 175 original browser identities and the same five skips as reviewed 114. The skipped cases include both `native-process-live` journeys, installed Hermes, installed Rivet file opening and the MCP appliance. They are not fresh live-execution evidence. The machine receipt records their exact IDs and all 15 Python-stage results; focused, coverage and broad invocations overlap, so their counts are not summed into unique tests. Release/native coverage was 85.34% against the required 85% threshold.

The corrected OCI helper freshly scanned existing immutable image `sha256:336e4f0d0d479777da9a482586e779e52e286981f1ac04d905c8ab719226569f`, built from `40b1d972`. It reported zero findings under the unchanged fixable High/Critical policy using a fresh database. This did not rebuild the image. The gate also emitted and validated a fresh wheel and sdist; their filenames, sizes and hashes are retained. They are local artifacts, not published packages.

Ruff, required frontend checks, the blocking 40-file MyPy scope, strict docs and distribution validation completed. Broad MyPy stopped on duplicate `conftest` modules in the existing nonblocking warning mode. Three React-hook lint warnings, Vite configuration/chunk warnings and Python resource/deprecation warnings remain recorded. Python skips and the one performance deselection are preserved per stage; no optional/external qualification is inferred.

**Source was clean before the gate and was not clean afterward.** The sole generated change was `runtime-extra-lock.json`'s `uv_lock_sha256`: the committed Git-LF digest changed to the actual Windows CRLF checkout digest. Exact generated bytes were preserved with SHA-256 `21b215a971c189f30a649f86729146a219a1b7fe1c75d4bc7218be1c68edbf4a`. Independent review confirmed the sole-field difference. At 09:55:57.1897496Z the coordinator separately restored that file and observed clean source. The original gate's `source_clean_after=false` record remains unchanged.

This projection copies no raw logs, image environment/history, report payloads or browser attachments. `raw-evidence-hashes.json.txt` binds retained originals by filename, size and SHA-256; the bundle manifest binds these public files. Private generation code and origin mappings stay outside `public/`.

H2 GitHub CI, the required push gate, published dev verification, human acceptance and production release are separate pending evidence. Earlier failures and the interrupted `fcb26a48` gate are preserved, not overwritten by this successful local run.
