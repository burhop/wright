# Independent packaged predecessor recovery closure

Reviewer `/root/native_candidate_review`; actual independent read-only probe passed at `2026-09-05T06:08:37.603373+00:00`.

**The T029 actual-packaged-predecessor gap is closed.** The exercise fulfills the bounded schema16/17 reader, backup, distinct-root restoration and forward-reopening obligation in079 implementation appendix106. No new actionable P1/P2 finding was established. Docker collector verification and final gate/freeze/review obligations remain separate.

Exact predecessor: commit `7404a549ae244cc05d89e062c60276e8862f53c9`, tree `9dcf9017c09b7df6b5eb96c5dae6c2c981e4e1c8`, wheel SHA-256 `89b45f2cbe7a797385c27a5fd0236e8a4c80db605735555c00e5afb0fe4c4f2b`. Exact current candidate: commit `40ebc645ddb641503706dcb3a7d9c84a2b685359`, existing wheel `d968c2fa6b7d23292340b51caacfb2ebcc1126fe02b8298c278a52d3a002e9d7`. Both carry0.1.9; the actual commit/tree/archive identities distinguish their schema16 and17 behavior. The coordinator built the predecessor once from its clean exact source with ordinary Hatchling packaging and reused the already reviewed current wheel. This review did not rebuild either package or run another lifecycle test.

I inspected the coordinator's retained launcher, exact current installed-native probe, boundary probe, all five phase outputs, public projection and actual retained databases. The current probe is byte-identical to the committed40 source (`fbf4a3f8051c54691cc86cd73ce3b991c12b773758c86959a20928f16ad43966`). The boundary probe (`41b039deaf001cf0c86c01b77a30f6e9b15ffe89c486e92779f9bdf650d463b2`) imports the real selected wheel before data-vault/compatibility code, asserts origins beneath that extracted package and never simulates the old reader by slicing current migrations.

The actual five phases initialize schema16 and a legacy workspace using the predecessor; upgrade with current code and perform a real saved native run/artifact download; retain a verified17 backup; reject17 with actual predecessor status and upgrade readers while preserving database/WAL/SHM bytes, then restore the verified16 backup to a separate root; and reopen current17 document/run/events/artifact with exact replay. The two current native phases collected30 and12 local native spans, respectively, and retain the same run ID and expected artifact SHA-256. Network denial and282 current package module-origin assertions are enforced by the unchanged installed-native probe. The old/restoration phases each assert42 package-sourced module origins.

Independent archive inspection verified1320 hashed RECORD members across the two wheels. All86 distinct relevant actual import-origin/compatibility member checks match both the extracted files and exact Git source, allowing only ordinary CRLF/LF checkout conversion. All five logged phase result objects match the raw report and explicitly selected public fields; all six public raw log/report hash and size references match. I separately opened the retained17 database, restored16 database and verified17 snapshot with SQLite `mode=ro&immutable=1`, verified schema/legacy workspace and native document/run/artifact counts, and confirmed that inspection did not change their bytes. The separate restored16 root has no native tables; both17 roots retain native work.

The public projection SHA-256 is `007c4310c0def25979cb341a29bca4434b213fb52ce6f22f6c263d036667c42e`, at `docs/programs/engineering-process-platform/evidence/reviews/native-predecessor-recovery-20260905/package-recovery-public-projection.json.txt` in the parent checkout. It accurately limits the claim to actual extracted-package subprocesses with the existing third-party Python environment. It does not claim a published release upgrade, complete installer replacement, other platforms, full-container backup/restore, downgrade or merging roots. The initial Git ownership check failure and rejected scratch workspace inside the repository are preserved; moving only the disposable data root to `D:/TEMP` did not modify product or probe policy.

Raw evidence remains under `D:/repos/wright/.local-run/native-process-milestone/native-predecessor-recovery-evidence/run-20260905T060125Z/`; the predecessor wheel is in the sibling `predecessor-dist/`. Reviewer artifacts under `D:/repos/wright/.local-run/native-candidate-review/.local-run/`: `review-packaged-recovery-40ebc645.py`, `review-packaged-recovery-40ebc645-result.json` and console output. No parent files, package bytes, databases, source or writer evidence were modified. This report closes the first of the two bounded gaps in `independent-t029-assessment-ea9ad268.md`; it makes no task-checkbox or whole-feature completion claim.
