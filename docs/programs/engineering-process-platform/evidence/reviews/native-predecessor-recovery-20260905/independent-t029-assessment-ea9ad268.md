# Independent T029 packaging/compatibility assessment

Reviewer `/root/native_candidate_review`; current inspected checkpoint `ea9ad2680fe0b92b9388025162d6fd2eaccce2e4`, tree `79dd6346a4ea8bef87a480b7b3ba7a2c6c4d86f1`. This assesses technical evidence for T029; it does not alter tasks or claim final acceptance.

**T029 is not yet fully verified.** Fresh packaging, native offline lifecycle, Docker execution/restart and legacy regression evidence is substantial and valid. Two specifically declared evidence obligations still require a bounded supplement: actual packaged predecessor recovery, and Docker native-span collection/linkage. These are verification gaps, not established product defects. Do not change the retained26-delivered/six-pending freeze partition on this report's basis.

The registry's Q-PACKAGING scope is `src/wright_engineering`, `packages/data_vault/src/data_vault`, and `docker`. Independently derived scope SHA-256: `d0350f076537ba865b4ff1d50770a8fcb112134e0e22f9bbb1aec6a8f3567616`. All three exact source trees remain identical to tested40. The later114 correction changes two browser test files only; it does not change any shipped or packaging behavior. Scope rows are retained in `review-t029-scope-ea9ad268.json`.

## Evidence already supported

| Obligation | Supported result and limits |
| --- | --- |
| Fresh wheel/sdist and web assets | Independent exact40 archive review verified674 wheel RECORD rows,669 wheel/672 sdist source files,13 manifest entries and six referenced chunks, requirements/extras, removed dependency chain and source correspondence. Wheel SHA-256 `d968c2fa6b7d23292340b51caacfb2ebcc1126fe02b8298c278a52d3a002e9d7`; sdist `6bca3121b4af84aec5101451bd326f3fbfeb7963c73971717524c479f5dcfcca`. Coordinator's exact40 build/clean-install stages passed before the later browser failure. |
| Native offline lifecycle and tracing | Existing wheel-content test invokes the installed-native harness against the actual built wheel in fresh isolated Python subprocesses, with network denial and package-origin checks. It verifies create, restart, package relocation, document/run/events/replay/artifact retention, in-memory native spans, and execution-to-artifact span linkage. Its source explicitly excludes different-version installer updates; package relocation is not that proof. Exact40 preceding package/native test stages passed under the existing gate. |
| Docker/offline/native/browser | Independently reviewed image `sha256:2347e29360668db4d6c67bc9475e9ca9ddd847fe5d8d56086c682505d0f1ba19`: standard cold network-none API/Hermes startup, packaged schema, three real native examples/four artifacts, failed/linked-corrected run/fifth artifact, restart and idempotent replay. Two actual browser journeys used bridge-loopback networking. No browser-offline or fresh container backup/restore claim. |
| Migration transaction and database recovery | Repository tests exercise interrupted17 migration, verified16 backup restoration to a separate root, rejection without rewriting, and reopening retained native work. These are valid current-code tests using `MIGRATIONS[:16]` to model the predecessor, and do not identify or execute an actual predecessor package. |
| Legacy non-interference | Existing gate preserves and exercises read-only078, Rivet/editor/runner and API/workspace compatibility. Installed-wheel legacy checks verify offline packaged reader content and Rivet assets/entry points/import origins; the standard Docker smoke separately checks offline Rivet editor/runner assets. Native runtime source remains independent of Rivet-specific serialization/runtime. This preserves legacy behavior and does not claim conversion or retirement. |
| Host and provenance limits | Actual native host is Windows; actual Docker image is Linux amd64 on Docker Desktop. Other native OS/architecture and OCI CI results remain distinct later obligations. The raw Windows uv.lock hash differs from the copied Git-LF record in Docker, while the native builder regenerates from raw checkout bytes; this is explicitly classified generation-input provenance in the separate archive report. No equality across those different raw inputs is claimed. |

## Required bounded supplements

1. **Actual packaged predecessor boundary.** `specs/079-wright-native-authoring/contracts/implementation-appendix.md:106` requires actual packaged predecessor/build identities before support claims. The original authoring checkpoint explicitly left this pending. No later retained evidence examined here supplies it. Build or use one wheel from exact predecessor `7404a549ae244cc05d89e062c60276e8862f53c9`, record its Git tree and archive hash, and reuse the exact40 wheel. Execute the real predecessor's reader/migration compatibility code from that package, not current code with a shortened migration tuple. Show schema16 initialization/backup, current17 upgrade and native work, predecessor rejection of17 without changing the newer database, restoration of the verified16 backup into a distinct root, and successful current-package reopening of the retained17 document/run/artifact state. Verify relevant imports originate in each selected package. Both builds may declare0.1.9, so commit/tree/archive hashes must distinguish them. The contract does not demand a published-release predecessor; an exact dev predecessor package satisfies the stated identity boundary. Do not infer a full public installer-version matrix from this database recovery proof.

2. **Docker tracing under a local collector.** Appendix118 explicitly places packaged/native and Docker tracing verification in T029. Native installed-package evidence asserts actual collected OTel spans and execution/artifact linkage. Exact40 Docker evidence currently asserts request trace IDs in immutable runs/events/artifact provenance with remote telemetry disabled; that does not by itself demonstrate actual OTel span emission/collection. Supply another retained exact-image collector proof if one exists, or run one small offline/in-memory-collector probe inside the existing exact40 image. Verify native run/artifact spans and linkage, bounded safe failure attributes and no remote export; keep this diagnostic process distinct from the already passed standard-startup proof. No new image or product build is required merely to collect that evidence.

Useful existing harnesses are `packages/data_vault/tests/test_native_process_repository.py::test_migration_retains_verified_predecessor_and_forward_native_work`, its interrupted-migration test, and `tests/native_runtime/test_native_process_lifecycle.py::verify_installed_native_lifecycle` / `_installed_probe`. Reuse their actual invariants while replacing the simulated predecessor with the real packaged one. No broad duplicate suite or product change is requested by this assessment.

The coordinator is preparing the predecessor supplement. Once both obligations are supported, one evidence-only T029 consolidation can bind the current unchanged source, actual artifact identities, preserved failures and host limitations. Q-GATES, independent final freeze/review, human study, CI, dev integration/deployment and final reporting remain separate; no task checkbox is changed here.

Sources reviewed include current079 tasks/plan/contracts, Q-PACKAGING definitions and four historical inconclusive attempts, authoring and package checkpoints, the actual migration/installed-native/installed-Rivet/wheel-content tests, the independently reviewed exact40 distributions, and exact40 Docker raw/public evidence. No test, package build, install, Docker command or security scan was repeated for this assessment. See `independent-package-provenance-review-40ebc645.md` and `independent-docker-projection-review-40ebc645.md` for exact bounded proofs and limitations.
