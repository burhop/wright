# Exact Docker candidate 40ebc645 — public evidence projection

Prepared by the Codex native_editor verification agent at `2026-09-05T05:07:10.066623+00:00`. This is an explicitly selected public presentation, not the full raw report, raw responses, traces or runtime configuration. The new verification tested commit `40ebc645ddb641503706dcb3a7d9c84a2b685359`, tree `29f573a09d7e1cbe60351eb21065105286b008bc`. One standard Docker build was made from the clean isolated source; no rebuild or publication occurred.

- Image / manifest list: `sha256:2347e29360668db4d6c67bc9475e9ca9ddd847fe5d8d56086c682505d0f1ba19`.
- Linux amd64 manifest: `sha256:d3cc70c2eae6fe5bb96886ff3ef4e4ad25d382ba5c6a2e51bd631861e36767f5`.
- Image configuration digest: `sha256:9e0e8e909b10688c39dfb3570eeb5166d3d2422bd5d3069a997fe63e4b8feda3`.

**Passed:** standard cold network-none startup with Wright API and Hermes health/connection; packaged schema discovery with 12 operations and no specs-tree fallback; three real native examples with four artifacts checked against expected bytes, SHA-256, size and provenance. A separate 100 g limit produced `ASSERTION_FAILED` and no artifact; changing the limit to 200 g produced the linked 135 g result and one additional verified artifact. Container restart retained documents, immutable runs, events and all five artifacts, including the original failure; exact retries replayed existing runs without duplicate history. These checks used the standard entrypoint and supervisor without a diagnostic startup override.

**Dependency removal verified in the actual Wright container environment:** `python-jose`, `ecdsa`, `pyasn1`, `rsa` and `six` are absent from installed distributions and importable modules. Wright metadata omits python-jose. `cryptography 50.0.0` and `PyJWT 2.13.0` remain present. This checks Wright's `/workspace/.venv`; it does not audit Hermes or optional ecosystems.

**Lock byte provenance:** this clean Windows checkout and the image's source `uv.lock` retain 2,082 CRLF endings and raw SHA-256 `f4a704d52588202bf157ab3f7ba49be3172b8a33d86c4a399d95c7898a9361f6`. The copied packaged runtime lock records the Git LF source hash `284093f806d36c6b91aa7ffb057e03fad2bdc32ca18f8592ad1e0c299a79a3e1`. Dependency resolution was not changed by this read-only identity probe; the parent separately tracks the cross-host build provenance finding.

**Actual Chromium journeys:** 2 passed,0 skipped in 28.356s, starting `2026-09-05T04:56:51.968Z`. Node24.19.0 / Playwright1.62.1 / Chromium151.0.7922.34 exercised the same image through the real Docker unlock form. All three examples, save/reopen, execution, artifact preview/download bytes and digest/provenance, mass failure and linked correction, and reload retention passed without mocked traffic. The corrected mass-check view had no serious/critical Axe findings and its journey reported no page errors.

**Limits and retained history:** the browser used a separate bridge container with its host port bound only to loopback; it was not offline. Earlier9b evidence established that this Docker Desktop host did not expose the port on an internal network, so that failed setup was not repeated here. Earlier9b browser attempts that lacked the unlock step and its scratch-module-format error remain preserved as historical evidence, not failures newly observed on40. Earlier99 schema-import build failure and eb cold-offline dependency-sync failure remain retained. No new product or harness failure occurred during this candidate's checks. No human-study, other-platform/architecture, engineering MCP qualification, real paid/provider call, registry-publication or fresh backup/restore credit is claimed. Smoke ScenarioE only references historical Phase8 work. Parent-owned full development gates and fresh dependency audit are separate evidence.

Task-owned containers are stopped; the image and disposable volumes remain retained. `docker-browser-public-projection.json.txt` contains selected identities, counts, outcomes, artifact hashes and bounded provenance only. `raw-evidence-hashes.json.txt` contains original relative filenames, hashes and sizes only. Raw responses and traces stay unchanged in private local evidence storage; no environment, authentication or revision-fingerprint values are copied into these public files.
