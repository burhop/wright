# Independent current-source Q-PLANNING acceptance

**Result: PASS for Q-PLANNING, T001 and T002 only.** Reviewer: `native_candidate_review` (`/root/native_candidate_review`), an independent technical agent that authored none of the implementation or planning contracts. No actionable planning inconsistency remains in the assessed scope. This is a present-source planning review, not a backdated preimplementation review or a final implementation approval.

## Exact subject

- Candidate commit: `60ef8672f1f61c2f4942e618638ec8901e9aa9a0`.
- Candidate Git tree: `7b5747eb5cf79f8193c8561eea70dc813c4eaa50`.
- Registry check: `Q-PLANNING`, “Baseline and independently reviewed contracts”, technical review, verification stage, independent reviewer required, tasks `T001` and `T002`.
- Declared source scope: `specs/079-wright-native-authoring` (43 committed files).
- Scope Git subtree: `70c81bd0ee6c4da0dda9d6a9a6c95908904a3899`.
- Publisher-compatible scope SHA-256: `0f32facf01d293314af410c5ba849ee7ba1a5b100a03898d6f179239d08b42d4`.
- Baseline: `7404a549ae244cc05d89e062c60276e8862f53c9`, tree `9dcf9017c09b7df6b5eb96c5dae6c2c981e4e1c8`.

The reviewer read the current spec, plan, task plan, milestone decision, analysis, research, data model, checklist, quickstart, historical proposal prefaces, language/semantic/schema/implementation/runtime/dashboard/human contracts and their concrete fixtures. Context outside the declared Q-PLANNING subtree included the recorded standing user goal, current work registry, program state, roadmap, AGENTS.md and the prospective scoped-delivery lifecycle rule. These contextual reads establish planning consistency; they do not silently expand this check into implementation, deployment or human acceptance.

## Planning acceptance rationale

1. **Baseline and authority are reconciled.** The retained standing goal requests the complete native create/configure/connect/save/reopen/validate/run/artifact/recovery outcome, a small development set, real local tool integration, the existing dashboard and eventual dev delivery. It explicitly authorizes bounded implementation and prospective governance decisions while excluding direct dev pushes, main/release and broader external effects. Current AGENTS.md points to 079. Program state identifies N01 and the exact baseline. The roadmap records F02 as integrated while explicitly retaining its missing deployed-browser proof. The 079 decision and historical proposal prefaces preserve earlier manual-only scope and historical approvals without presenting them as current restrictions or inventing exact-subject approval.
2. **One official process language is explicit and implementable.** FR-001/FR-002, the language-authority contract and the plan require the Wright document, versioned schema, operation signatures and semantic validation to govern AI/programmatic clients, canvas/Inspector, readable text, API, HTTP CLI and runtime. Exact IDs and endpoints survive label/layout changes; presentation, bindings and immutable run evidence remain separate. Create/update/check use the same authorization, validation, CAS and diagnostics. Frontend validation and renderer state cannot override backend semantics. An editable DSL is a possible later lossless frontend; autonomous AI authoring remains deferred, consistent with the goal. Simulated client conformance is not represented as actual AI study evidence.
3. **Native replacement is concrete and legacy preservation remains required.** New native work must avoid Rivet serialization/editor/runtime and example-specific dispatch while reusing generic service boundaries. The frozen F02 reader is not silently made writable. Native milestone delivery is distinct from Rivet conversion and retirement, which require later capability inventory, unsupported/lossy-case reporting, new native identities, preservation of originals, equivalence/recovery evidence and explicit removal scope. As a bounded baseline check, the 078 contract subtree, packaged immutable process-definition assets and complete integrations/rivet subtree have identical Git identities at the baseline and candidate.
4. **The contracts resolve the necessary implementation decisions.** They freeze the bounded document schema; exact structural/readiness separation; 12 versioned operations; decimal/unit representation and arithmetic; atomic command/buffer/history behavior; workspace-scoped API and idempotency; coordinator ownership; terminal races; staged/indexed artifacts and integrity; exact MCP binding; and predecessor rejection/retained-root recovery. Thin API, domain/persistence/application boundaries, UI accessibility and trace requirements map to stable tasks. Explicit deferrals prevent the initial milestone becoming an open-ended language/runtime rewrite.
5. **Examples are useful development inputs, with separate qualification.** The three outcomes are document creation/review, a quantity calculation with units, and artifact checking/revision. Frozen oracles specify actual expected content and negative cases. The failed mass variant and separate study-trace fixture support recovery and comprehension checks. Planning does not claim these declarations are successful runtime outputs. The goal, language/dashboard contracts and task T026 keep development fixtures, local computation, live MCP, public demos and eventual benchmark qualification distinct; native delivery does not grant any of the 100 benchmark cases credit or reveal a blind holdout set.
6. **The 32-task partition preserves all remaining obligations.** Current task text contains exactly T001–T032, with T001–T026 checked and T027–T032 unchecked. The implementation partition is baseline/dashboard, shared semantics/examples, authoring/persistence, runtime/artifacts/recovery and real-tool/example work. The remaining partition is exact-candidate independent review, actual independent human study, cumulative packaging/compatibility, gates and terminal CI, dev integration/deployment, and final truthful reporting. The scoped-delivery rule permits an independently useful implementation PR without claiming whole-milestone acceptance. It retains independent review and every required technical/CI gate. Human study, deployed-build observation and final reporting cannot be erased by a scope partition or aggregate percentage. T031/T032 alone are exempt from the integration denominator with explicit postintegration/self-reference reasons; implementation and verification denominators stay 32 and integration stays 30.
7. **Human and completion criteria remain substantive.** The preregistered five-participant protocol supplies exact H1–H5 tasks, fixtures, time limits, scoring, assistance/dropout rules and retention of failed trials. It requires at least four successes per task, final candidate identity, independent people and separate manual accessibility evidence. H5 uses the existing standalone implementation dashboard at port 8765. Technical agents and automated accessibility results cannot become human acceptance. AC01–AC10, task dependencies and the final lifecycle projection preserve actual gates, integration and deployment requirements; benchmark, commercial and release readiness remain separate.

## Independent bounded checks and limitations

The retained read-only probe `review-planning-60ef8672.py` passed. It independently derives the declared scope digest using the publisher's sorted Git-tree-row canonical JSON algorithm, confirms the fixed task partition/denominators and current baseline/AGENTS pointer, checks JSON Schema validity and equality with the packaged authoritative schema, and validates all five development/negative-runtime/study documents for schema, global IDs, endpoint direction/type/cardinality, one producer, DAG and artifact-output references. It confirms three packaged definitions match their contract fixtures, three explicit oracle/negative pairs, eight structural/draft fixtures and the presence of five accepted/nine rejected canonicalization vectors. The vector presence check does not claim another execution of the canonicalization suite. The immutable F02 and Rivet subtree checks are source-preservation evidence, not new legacy runtime tests.

This planning PASS is a distinct review result and may support Q-PLANNING only. It does not grant other quality checks, mark T027–T032 complete, assert a complete full gate, or substitute for a frozen final candidate review, actual humans, deployment or release evidence. Existing historical planning findings and their original observations remain intact; the present report independently reassesses the current resolved contracts without changing their dates.

Retain this report and probe under program evidence, outside the declared 079 source subtree. The current Q-PLANNING scope includes the entire 079 directory, including task checkboxes and existing evidence. Any later change inside that subtree changes its exact scope digest, even if it is status-only; recheck the bounded difference and bind the planning record to the new identity before claiming current-source planning credit. A report-only addition under program evidence does not change this planning scope.
