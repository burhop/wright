# Raw evidence attachment names

Unschematized JSON attachments use a `.json.txt` suffix, keeping tool output distinct from schema-governed program records. Two browser captures are explicitly derived review projections: `actual-native-example-runs.json.txt` and `actual-native-runs.json.txt` omit only five non-secret snapshot revision fingerprints. See `projection-note.md` and `projection-manifest.json.txt` for exact pointers and original/projection hashes. All original captures remain unchanged locally. Other raw payloads are retained apart from Git text line-ending normalization.

For each original `.json` or selected `.log` filename mentioned by the checkpoint or selected-evidence manifest, append `.txt` to locate its retained attachment here. Original external Docker-manifest filenames still identify the original Docker evidence directory; that manifest is not a claim that every original Docker log is copied into this directory. Markdown and PNG names are unchanged. Original-byte hashes in the retained manifests continue to describe the original external files, not the two projections. A separate committed attachment map binds the final retained paths and Git bytes.

All unpublished pre-projection metadata commits remain available locally under `refs/codex/evidence/native-metadata-before-public-projections`; they were not published. Only metadata after the validated freeze was reconstructed with the labeled projections. The frozen implementation, review subject and full gate remain candidate 60ef8672. Scanner configuration and policy were not changed; the pinned offline projection scan passed with zero findings.

The first unpublished evidence commit (`96f36a9e6d21001114b8386a6f80855292f4c54e`) correctly failed validation when raw JSON outputs were interpreted as typed program inputs. That commit is retained at local ref `refs/codex/evidence/native-raw-json-invalid`. Corrected evidence commit `4d2633d0e4136262d38eb29e9bfb37cd3fc24bc0` passed committed-source validation after suffix changes. No validator, schema, payload, candidate code or test result was changed to obtain that pass.
