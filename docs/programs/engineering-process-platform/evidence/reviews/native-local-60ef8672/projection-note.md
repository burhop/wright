# Derived browser evidence review copies

These two files are presentation copies, not unchanged raw API responses. Exactly five snapshot.token values are replaced with explicit omission markers. They are non-secret document revision CAS fingerprints; they confer no authorization. Every other JSON value is unchanged. Original proof files remain unchanged locally, with additional byte-identical copies retained outside the parent worktree. and projection-manifest.json.txt records original/projection hashes and the exact omitted JSON pointers. No scanner rules or exclusions were changed.
