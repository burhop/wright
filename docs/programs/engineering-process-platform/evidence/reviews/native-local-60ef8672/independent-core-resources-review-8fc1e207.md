# Independent native resource and fixture correction review

Reviewer: `native_candidate_review`, independent of the implementation authors. Reviewed correction: `8fc1e2078aa623983e043f8b4706b9fe0784337e`; Git tree: `5fd7e556da3b1d0c95944e7ba4f29ab83d136ee9`. Also inspected test-only corrections `03830186c38be75dc92f66ca835e6a26560d67da` and `27b676a5da1b3c8972839475bb477df4cbd631a2`, and documentation change `90c8a32fa52a7fbc8b52110b76960c0cf1783305`.

No actionable P1/P2 finding was established. The production change only replaces schema lookup through the module's filesystem path with `importlib.resources.files("core")` and removes a redundant `PureWindowsPath.drive` rejection. The same packaged schema, strict JSON parser, schema validator and cached contract remain authoritative. A Windows drive prefix necessarily includes a colon or begins with a slash/backslash, which the remaining checks still reject. Dot segments, empty segments and NUL rejection remain unchanged; no filesystem access or workspace authority is added to core validation.

The independent retained probe `review-core-resources-8fc1e207.py` passed. It creates a ZIP containing the exact correction's committed core package and imports it in a fresh isolated Python subprocess. It compares against the previously reviewed directory package at `53ec6694250e6235a453251c951ff7da26043e7d`. All four retained example documents (`concept-brief`, `mass-check`, `mass-check-fails`, `package-review`) retain identical semantic digests and readiness findings. The complete discovered language contract is unchanged and its schema equals the exact committed package resource. Contract copies remain isolated from caller mutation. Setting `native_process.__file__` to a nonexistent location before discovery does not break ZIP loading. Sixteen additional lexical edge cases, including Unicode names, reserved DOS names, separators, controls and colon forms, retain the previous path acceptance/rejection behavior. This does not redefine the established lexical contract or claim runtime execution of the examples.

The new author tests add directory/ZIP package discovery, the shared 21 frontend path vectors and comparison with the prior parser. The dummy-token label is test data only. The dashboard fixture correction retains the existing visibility, refresh, recovery and accessibility assertions while matching the retained F02 fixture's label. The milestone fixture now explicitly constructs an empty-evidence case rather than accidentally importing live checkpoint evidence; it continues requiring zero verification/integration credit, also exercises fully implemented tasks with absent evidence, and adds missing/extra/different-identity attestation rejection. These changes do not weaken the intended coverage. The docs change directs the existing plan link to the repository dev location and does not alter implementation or acceptance rules.

No full suite, frontend build, wheel build, Docker build, browser run or package install was repeated. Author-reported test totals remain separate evidence. The new core source must be included in the next final artifact build and full gate; earlier artifact hashes identify the prior source. This is a bounded technical closure, not the final typed independent approval, human acceptance, dev delivery or release authorization.
