# TR-0108 manifest defect and supported remediation assessment

**Open metadata blocker:** `TRANSITION_MANIFEST_MISMATCH` at `f86c2ccf3e09c4a629b4f018bec4db17c1604ae1`. Its actual introducing commit changes `work-registry.json` and `specs/079-wright-native-authoring/tasks.md` in addition to the declared state paths. Those two paths are absent from TR-0108's manifest. Independent read-only probe reproduces the exact mismatch; product candidate `0aae81db`, its freeze and typed technical review are unaffected.

My earlier f86 bounded review checked task normalization, artifact/scoped identities and state/input/output digests but missed the introducing commit's complete path set. Its blanket metadata-pass conclusion was incomplete and is superseded by this blocker assessment. The separately established planning bytes and acceptance evidence are unchanged; they cannot override the authoritative validator failure.

There is **no existing generic append-only correction** for this defect. `scripts/program_control/validation.py:2719` belongs to the fixed V9/TR-0051 correction profile. Its corrected-manifest target relaxes only historical list ordering. At lines 2991–2999, complete declared paths must still equal actual container paths. A new correction document cannot bypass that equality; modifying TR-0108 in a later commit would additionally violate `TRANSITION_BLOB_CHANGED` at line 3014. The coordinator state-machine contract also explicitly requires the complete changed-path manifest.

The smallest route without validator/policy changes is a bounded reconstruction of the **unpublished metadata suffix after valid `56dc634a`**: preserve original commits, failed validation and this missed-review finding under private retained evidence references; correct only TR-0108's original manifest to include both omitted paths; rebind affected suffix identities; then validate the new clean attached source with the unchanged authoritative CLI. Keep source/freeze/typed-review and actual T027/T029 completions intact. This is not an append-only correction on the faulty branch and must be reported honestly as unpublished metadata reconstruction with retained originals. No broad product/history rewrite is needed.

Probe/result: `review-tr0108-manifest-gap.py` and `review-tr0108-manifest-gap-result.json`. No rewrite, code change, gate suppression or passing-source claim was made by this review. The in-progress push gate remains unpassed.
