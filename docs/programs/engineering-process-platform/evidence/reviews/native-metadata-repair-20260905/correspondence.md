# Prospective native metadata reconstruction

This reconstruction prepares a private replacement for the twelve unpublished metadata commits after `7caca82d`. The owning branch remained at `08a7135e2c0777f8ecf35c897d86bb3a68ce5860` throughout preparation. Its complete original suffix is retained at `refs/codex/evidence/native-before-manifest-hook-repair-08a7135e`. The original reconstruction script and written historical reports are unchanged.

Prepared tip: `de4189230bf13699bfd8f80a960ba79057baf4ee`, at `refs/codex/candidate/native-manifest-hook-repair-ready`. `complete-commit-map.json.txt` records every old/new commit, full tree, program tree and rebound full identity. `writer-correspondence-checks.json.txt` records the exact transition-container path comparison. No branch push, product gate, deployment or owning-branch adoption was performed by this preparation.

The two initiating corrections are narrow:

- Rename the live browser case map's `auth_addition_lf_sha256` key to `source_hook_lf_sha256`. Its existing digest and all remaining values are unchanged. The independent source/scanner proof establishes that this is a digest of the public source hook. No scanner policy changed.
- Correct `TR-0108.git.changed_paths_manifest` to include the work registry and 079 task file actually changed in its original introducing commit, along with the state snapshot and current state. The four entries are sorted. The transition itself is excluded according to the unchanged validator contract.

Dependent structured JSON commit/tree/blob, public-artifact SHA256 and canonical JSON digests were rebound to their actual prepared counterparts. This is metadata correspondence, not a new execution. In particular, the rebound `b699191d` identity identifies the aggregation of existing proof: the full script still ran against source40 and failed, while the corrected browser stage ran against source114 and passed. Those exact execution commits, outcomes, IDs, counts and raw artifacts remain unchanged. Fields under `original_*`, `raw_*`, `private_raw_*`, `*_provenance` and `provenance` were protected. Written Markdown reports and every product, dependency, test, task and other non-program byte are identical between each original/reconstructed pair. The current task file remains 28 checked tasks; the frozen review partition remains 26 included and six pending.

The reconstructed frozen candidate is `7f55b31cb754421a605f2e7aff1d676ba501606d`, tree `ebce3a4a68aaa62cff8b50444a727ef70233ba2d`, with freeze108 at `94731459ab1b26d8885a0b398fdd430c75fed4b5`. The old `NATIVE-REVIEW-20260905-0AAE81DB.json` remains byte-for-byte intact as historical evidence. The independent reviewer issued the new `NATIVE-REVIEW-20260905-7F55B31C.json` at the actual time `2026-09-05T07:01:08.066459Z`; its bytes were inserted unchanged. States109/110 and the current Q-REVIEW attempt7 use this new record and actual reviewed subject/time. The original attempt7 row remains in the preserved original suffix; its old verdict was not silently attached to a new unreviewed identity.

The five review-dependent replacement commits use their actual new committer times. TR-0108 and TR-0109 record actual metadata-assembly times after the new review. Original authored records/times remain available in the preserved suffix, and the full map explicitly retains their historical transition times. The first seven existing metadata transformations preserve original commit author/committer metadata; their new Git identities denote the documented reconstruction, not a repeated test or decision.

The unchanged authoritative CLI passed on clean attached freeze947, exit0, from `2026-09-05T06:57:20.296826Z` through `06:57:38.656695Z`, and on clean attached prepared tipde418923, exit0, from `2026-09-05T07:03:48.891355Z` through `07:03:52.929864Z`. Both stderr files are empty. Raw reports and exact command observations are retained beside this note. These are bounded program-validator results; they do not change the failed original08 validation, failed full40 script, or pending required push/CI/dev/human results.

Independent complete-reconstruction closure is required before adoption. The coordinator will retain the earlier failure/scanner evidence and final closure in an ordinary subsequent evidence commit, then continue the existing lifecycle and required gates. Human T028 and whole-milestone T032 remain pending; production and benchmark qualification are separate.
