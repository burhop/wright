# Independent classification of the exact40 WebKit failure

Reviewer: `/root/native_candidate_review`. Candidate `40ebc645ddb641503706dcb3a7d9c84a2b685359`. This records the original failure and bounded causal evidence; correction review and final candidate approval remain pending.

**Classification: test fixture isolation gap causing a layout shift during the mouse click.** No focus reducer defect is established. A passing standalone retry is not used as closure.

The terminal full merge gate failed: 169 Playwright passes, five skips, one WebKit failure in `tests/ui-integration/workspace-surfaces/focus-layout.spec.ts:17`. The test clicked the visible focus button but the workspace remained `normal` rather than `focus`. The original log remains failed and must not be relabeled as a passing full gate.

Independently checked retained parent evidence:

- `.local-run/dev-merge-gate-40ebc645-webkit-failed.log`: SHA-256 `312a22f2b314dc35e98085b551d9ceb205e790c36e40f968570e2ff4f0e5634b`.
- `.local-run/playwright-report-40ebc645-webkit-failed.html`: SHA-256 `d4bf193ea42c4fb291c4000046b4a888df55686f3ccceb7f2a17684f9cad0b02`.
- Original case trace and error context under `test-results/playwright/workspace-surfaces-focus-l-453cd--tab-switch-and-restoration-webkit-workspace-surfaces/`; hashes retained in `review-focus-failure-40ebc645-result.json`.

The original trace records focus click `call@390` at CSS point `(609.5, 231.84)`, with the input interval from monotonic `275298.040` to `275350.922` ms. Two requests to `/api/workspace/surfaces/surface-app/live-app` returned status 200 HTML instead of runtime JSON and completed at `275314.640` and `275314.776` ms, inside that interval. Immediately after the click, the DOM snapshot serializes the new error paragraph, `The string did not match the expected pattern.`, in both surface controls. Before/after screencasts show the resulting vertical movement above the focus button.

The source fixture `mockWorkspaceShell` supplies descriptors and presentation preferences but omits the runtime `/live-app` response. The real `LiveAppControls` fetch therefore escapes the mocked service fixture and receives the application's HTML fallback. Its error paragraph is rendered before the focus button in `LiveAppSurface`. The reducer correctly maps `enter_focus` to focus mode; the trace provides no indication that a successful button click dispatch was subsequently reversed.

The writer then constructed a controlled WebKit reproduction on the unchanged source: hold that runtime response, press the mouse on the button, release the same HTML response, wait for the resulting error paragraph, then release the mouse. I independently inspected its retained source, actual passing log (one causal reproduction, 3.9 seconds) and trace attachment. Button Y moved from `222.34375` to `244.734375`; mousedown targeted `BUTTON surface-enter-focus`, mouseup targeted `P`, and the resulting click targeted `DIV live-app-surface`. Mode remained normal as expected for a lost button target. Trace SHA-256: `f714a435553cb08f65f65654e07483b00f802b8ca511511879596de0c5b64966`. Evidence is under `D:/repos/wright/.local-run/native-process-milestone/native-webkit-focus-evidence/controlled-race-40/`. This resolves the event-target uncertainty in the original recording and supports the classification. I did not rerun a browser.

The anticipated bounded correction is to complete the mocked runtime contract, preserve descriptor lifecycle/instance/generation and test-specific overrides, and retain the original mouse interaction and all focus/resize/tab/restoration assertions. The exact patch and its adverse/positive proof still require review. Broad fallback mocks, extra retries, sleeps, forced clicks or replacing the pointer interaction would not constitute closure of this cause.

I read the exact candidate's `docs/contributing/dev-push-runbook.md`. Its delivery-speed rule permits a test-only correction with directly affected deterministic checks and one CI cycle when shipping behavior, contracts, dependencies, packaging, security policy and substantive gate coverage are unchanged. Its workspace-surfaces routing explicitly requires Chromium, Firefox, WebKit and the desktop profile for changed contracts. Conditional on the actual correction staying within that boundary, the already completed unchanged-source backend/frontend/package stage evidence should remain bound to40, the affected fixture consumers should pass across all four profiles, and the complete previously failing browser stage should be completed on the corrected candidate. The final evidence must explicitly describe the failed40 gate plus the corrected verification, not invent a successful exit for the original command. Any product or other substantive correction requires reassessing full-gate impact. Required CI and final push gates remain applicable.

Reviewer scratch artifacts: `review-focus-failure-40ebc645.py`, `review-focus-failure-40ebc645-result.json`, `review-focus-failure-40ebc645-output.txt`, and unchanged extracted before/after screenshots under `focus-failure-40ebc645/`, all in `D:/repos/wright/.local-run/native-candidate-review/.local-run/`. No source, test, policy, parent state or writer evidence was changed. Final typed approval remains pending correction closure, required verification and exact freeze.
