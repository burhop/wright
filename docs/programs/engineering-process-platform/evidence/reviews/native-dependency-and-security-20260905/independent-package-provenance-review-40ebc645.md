# Independent fresh archive and runtime-lock provenance review

**The fresh candidate40 archives pass bounded source, resource and metadata inspection with one explicitly derived provenance variation. No P1/P2 correctness or reproducibility-contract defect is established by this variation.** Candidate: `40ebc645ddb641503706dcb3a7d9c84a2b685359`, tree `29f573a09d7e1cbe60351eb21065105286b008bc`. Final full-gate/Docker acceptance and freeze remain pending. This reviewer performed no build, install, suite or container rerun.

The inspected files under the parent `dist/dev-merge-python/wright-engineering` are:

- `wright_engineering-0.1.9-py3-none-any.whl`: SHA-256 `d968c2fa6b7d23292340b51caacfb2ebcc1126fe02b8298c278a52d3a002e9d7`.
- `wright_engineering-0.1.9.tar.gz`: SHA-256 `6bca3121b4af84aec5101451bd326f3fbfeb7963c73971717524c479f5dcfcca`.

The strict reusable archive probe first stopped at exactly `wright_engineering/runtime-extra-lock.json`; its failure remains in `review-refreshed-native-artifacts-40ebc645-first-attempt.txt`. This was an overly strict assumption that a generated provenance field must retain the committed generation-input hash on every checkout. It was not treated as a product failure merely because two hashes differ.

I independently invoked the unchanged `stage_runtime_extra_lock()` function only on two isolated scratch fixtures using the same committed project manifest. The Git-LF lock is366,486 bytes, SHA-256 `284093f806d36c6b91aa7ffb057e03fad2bdc32ca18f8592ad1e0c299a79a3e1`; its generated record is exactly the committed JSON, SHA-256 `48cb93335ec3da98d23ecc1270394577929cafb3d9856d8aecb5b046f17de959`. The Windows checkout lock is368,568 bytes with2,082 CRLF sequences, SHA-256 `f4a704d52588202bf157ab3f7ba49be3172b8a33d86c4a399d95c7898a9361f6`; its generated record is SHA-256 `21b215a971c189f30a649f86729146a219a1b7fe1c75d4bc7218be1c68edbf4a`. Replacing only CRLF with LF makes the input files identical. The generated JSON objects differ only in `uv_lock_sha256`; all requirements, schema, distribution and version fields agree.

Both actual native archives contain exactly the independently generated Windows record. The sdist does **not** contain `uv.lock`; it carries the generated record as provenance. The unchanged builder deliberately hashes raw input bytes when generating that record. No production consumer of `uv_lock_sha256` was found. The existing artifact validator checks runtime-requirement/schema completeness and hashes the record payload; it does not define that field as the current container's raw-lock integrity check or impose cross-platform equality of generated provenance hashes. I found no frozen contract requiring raw checkout line endings to produce identical provenance fields. Treating generation-input identity as provenance is therefore consistent with the inspected implementation; this is not an audit-policy exemption or changed dependency resolution.

Docker takes a different, separately attributable path: it copies the committed record instead of regenerating it. The editor's retained offline read-only image probe reports a CRLF `/workspace/uv.lock` with raw hash `f4a704...`, while the embedded record retains generation-input hash `284093...`. Probe log SHA-256: `36aa5729b32c87f285da490260bfbf0e2b5e3674689c4fff4a3f6c8cb8368d53` at `D:/repos/wright/.local-run/native-process-milestone/native-docker-evidence-40ebc645/lock-identity-40ebc645.log`. I inspected this log and matched both hashes to the independently derived variants. The Docker record describes the committed generation input; its field does not equal the image's current raw lock bytes. The native archives describe the Windows regeneration input. Neither variant's hash is claimed to match the other raw input. Both inputs normalize to the same committed dependency lock. Complete Docker workflow acceptance is a separate review.

After explicitly substituting only that independently regenerated provenance resource in the expected build output, all remaining archive correspondence checks passed:669 wheel source/resource files and672 sdist files, with391/394 ordinary CRLF-only differences. This is not general JSON normalization or permission to ignore changed fields. All13 manifest-addressed assets and six referenced chunks are complete and byte-correct; web manifest SHA-256 remains `4775742dc1b7ed8f7993fc67c00eb7448a1ec908b255fcd97ef91250187851d9`. All674 wheel RECORD entries match their actual payload sizes and SHA-256 values. Wheel METADATA and sdist PKG-INFO requirements/extras match the exact candidate project manifest, and all five removed JOSE-chain requirements are absent. There are no unexpected nonmetadata wheel files or unaccounted sdist inputs.

No attribute or product patch is needed solely to force these provenance hashes equal. The evidence must accurately distinguish clean source at gate start from the builder's subsequent staging of the generated record. After terminal completion, restoring only that proven generated source file to its committed bytes does not change the inspected immutable archives, runtime code or dependency requirements; the actual Windows-generated record and both archive identities should remain retained in evidence. This review does not claim the checkout remained clean throughout the build or that a new artifact was tested under an older hash.

Independent proof/result: `review-runtime-lock-provenance-40ebc645.py` and `review-runtime-lock-provenance-40ebc645-result.json` beside this report. Both fixture inputs/generated outputs are retained under `runtime-lock-provenance-40ebc645/`. Parent-reported clean-install success remains parent execution evidence; this independent review inspected completed archives and source correspondence without repeating installation. Final candidate approval still requires terminal gate, complete exact Docker/browser evidence and freeze.
