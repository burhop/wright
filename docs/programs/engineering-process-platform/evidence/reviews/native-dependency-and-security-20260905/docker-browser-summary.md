# Docker and browser verification — explicit public projection

Prepared by the Codex native_editor verification agent from retained evidence. This is a concise presentation, **not the full raw report, raw API responses or browser traces**. Preparation at `2026-09-05T03:51:01.903355+00:00` did not run new product tests. Original result recorded at `2026-09-05T03:47:17.947682+00:00`; successful browser run started at `2026-09-05T03:43:06.087Z`.

The original tested commit was `9b63647988b781ff02eb780fff40af5ff4c793ab`, tree `f8c810065e2a2965edc0519e4cc54c45e3217a8c`. Its identity is preserved as historical evidence; any later publication-metadata mapping does not create a new test run. The image was built once from the clean isolated source using the standard Dockerfile, version `0.1.9`, local tag `wright:native-9b636479`. No tracked product changes occurred during verification.

- Immutable image / manifest list: `sha256:6776798e85c7b359f50cc3e1d474cc6c0d5282cfd5aff75e5ab1f07ba43b045b`.
- Linux amd64 manifest: `sha256:0f7bf21d41295d8f6e27750bb9abff809bfc8b86a1515fe4aa700830825b0dcf`.
- Image configuration digest: `sha256:79cd277c109229e238e4cc1856063ee2b9943c890cbbbc71dc035105e8b33555`.

**Observed pass:** standard cold network-none startup, Wright API and Hermes health/connection, packaged schema discovery with 12 operations and no specs-tree fallback. Three actual native examples produced four verified artifacts. Each artifact's bytes, SHA-256, size and provenance were checked. A separate 100 g maximum failed with `ASSERTION_FAILED` and no artifact; correction to 200 g produced the exact 135 g report in a linked run. The original failure remained immutable. Container restart retained documents, runs, events and five total artifacts; exact retries replayed existing runs without duplicate history. These checks used the standard entrypoint and supervisor without a diagnostic startup override.

**Actual browser:** 2 passed, 0 skipped in 26.059183 seconds, using Node 24.19.0, Playwright 1.62.1 and Chromium 151.0.7922.34. The same immutable image served the UI. The journeys exercised all three examples, save/reopen, execution, artifact preview/download with exact content/digest/provenance, failed mass check, linked correction and reload retention. The corrected mass-check view had no serious/critical Axe findings; that journey reported no page errors. The served license inventory records DOMPurify 3.4.14. No requests were mocked.

**Retained failures and setup limitations:** Docker Desktop's internal network kept both services healthy but did not assign the published host port; the browser therefore used a separate bridge container with its port bound only to loopback. The browser run was not offline. An initial scratch Playwright configuration failed before tests because its module format did not support `import.meta`; only the disposable configuration was corrected. The first two unchanged development-server browser tests then timed out on Docker's Unlock Wright screen before reaching the editor. A disposable test copy added the real unlock-form step, after which both journeys passed; the original failed reports and traces remain retained. Earlier candidate `99cee2dc` failed to build because a frontend schema import was absent from the standard Docker stage. Candidate `eb63344c` subsequently failed standard cold offline startup because dependency synchronization attempted downloads; its diagnostic-only workaround result remains distinct from this candidate's standard-startup pass. Historical `53ec6694` and `c7f1f218` results also remain unchanged.

**Limits:** Linux amd64 on Windows Docker Desktop 4.48.0 / Engine 28.5.1 only; browser verification used a bridge, while cold-start/schema/native-HTTP/restart proofs used network none. This evidence does not qualify engineering MCP servers, human usability, other operating systems or CPU architectures. It includes no real paid/provider call, registry publication or fresh backup/restore test. The smoke script's backup/restore line only cites historical Phase 8 work. Full development gates and dependency-audit policy evidence are separate. Task-owned containers were stopped; the image and disposable data remain retained.

The accompanying `docker-browser-public-projection.json.txt` contains only explicitly selected identities, counts, artifact hashes and provenance fields. `raw-evidence-hashes.json.txt` contains filenames, SHA-256 and byte sizes only, including unsuccessful attempts. Original files remain unchanged in private local evidence storage. No environment records, authentication material, revision fingerprints, raw traces or copied runtime/test configuration are published here. No scanner rule or exclusion was changed.
