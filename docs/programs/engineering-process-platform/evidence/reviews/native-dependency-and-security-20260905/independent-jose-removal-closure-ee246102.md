# Independent unused JOSE dependency removal review

**Passed; no open P1/P2 finding in `ee246102acc94ce07cd6d2e604496d299b49fbd3`, tree `86bff565b8b3344f7c507cb200202a145ac6e6d0`, based on candidate22.** Reviewer `/root/native_candidate_review` authored neither the patch nor its tests, installation or audit. This closes the bounded unused-dependency correction and its normative reconciliation; final combined package/gate/freeze acceptance remains separate.

The nine-file delta removes only two `python-jose` requirements, regenerates the lock and embedded runtime requirement record, and reconciles five documentation/constitution files. There is no authentication code, wire protocol, role, origin, cookie, token-storage, test, scanner or dependency-audit-policy change. The independent prior assessment parsed340 production/adapter sources and inspected dynamic loading; the unchanged runtime uses opaque bearer credentials and HMAC-derived browser sessions, with no JOSE/ECDSA import path.

Independent exact-data reconstruction proved the root/API manifests differ only by their unused JOSE declaration. The complete parsed lock equals the previous lock with exactly `python-jose`, `ecdsa`, `rsa`, `pyasn1`, and `six` removed and their four requirement/dependency edges removed from the two Wright records. All80 other remaining package records, including every version, source, artifact hash, marker, and dependency, are unchanged. There are87→82 package records and86→81 distinct names because NumPy has two platform/version records. An initial scratch assertion conflated records and unique names; its failed output is preserved in `review-jose-removal-ee246102-first-attempt.txt`. The complete lock comparison had already passed; correcting this counting assumption changed no product data.

The resulting `uv.lock` SHA-256 is `284093f806d36c6b91aa7ffb057e03fad2bdc32ca18f8592ad1e0c299a79a3e1`. The embedded runtime lock is byte-identical to the existing builder's sorted, unique runtime-requirement serialization and exact lock digest; its SHA-256 is `48cb93335ec3da98d23ecc1270394577929cafb3d9856d8aecb5b046f17de959`. Cryptography50.0.0, PyJWT2.13.0, passlib/bcrypt and all other remaining dependencies are preserved. Independently compared registry tuples show all73 candidate tuples are a subset of the78 in known public baseline7404; this is inventory correspondence, not an advisory result.

The writer's fresh CPython3.14.2 environment was created with `uv sync --frozen --extra runtime --offline`; its retained log records69 installed packages and an editable source installation. I independently inspected all69 installed METADATA records and their locked versions, the absence of the five removed module/distribution paths, explicit exclusion of system site packages, and the retained required cryptography/PyJWT modules. The writer's successful inline `find_spec`/`importlib.metadata.requires`/runtime-lock proof is consistent with that inspection. The writer supplied the original command payload retrospectively; there is no independently retained harness for that inline invocation, and my metadata/filesystem check provides separate corroboration. This demonstrates a clean editable runtime, not a built release wheel/sdist installation.

The retained focused test command was `.venv/Scripts/python.exe -m pytest apps/api/tests/test_security.py apps/api/tests/test_native_process_api.py tests/native_runtime/test_security.py tests/release/test_python_artifacts.py tests/release/test_build_native_runtime.py tests/release/test_dependency_audit_policy.py -q --basetemp=.local-run/auth-pytest`. The actual log records39 passes, one platform skip and139 warnings in21.92s. The test environment used isolated source paths, local test data/TEMP and a fresh secrets-file path. I inspected the actual log and relevant existing security checks, including missing/wrong/correct bearer authentication, session distinction, origin enforcement and native bootstrap; I did not repeat the suite. Test assertions and policy acceptance logic are unchanged.

The root subsequently ran a fresh PyPI advisory query for this exact patch and reports terminal exit zero plus a separate pass through the unchanged repository policy evaluator. I independently inspected the resulting90-package report: zero vulnerabilities, zero skips, none of the five removed packages, and coverage of all69 installed runtime/test-environment package versions. The additional audit-tool packages are included in the90, not presented as runtime dependencies. `pip-audit-ee246102-public.json` SHA-256 is `f8264f779c9bf1726f7c6f82b1742b2dee21a3767b94f4ea94aeea1066e97dc2`; its log SHA-256 is `300d7d85f883775ff9dec1c6fc1e47bba60caa7fb16ff16d7cc5e983c7605dd5`. This is the new audit evidence. The earlier retained inventory comparison remains explicitly labeled as historical and is not used as an audit pass. The expired exception has not been renewed, extended or otherwise used to waive a finding. The worker's initially rejected advisory query remains a failed attempt; the separately approved root query supplies the actual result.

Active constitution3.1.1 is a prospective clarification of the established opaque-token/session mechanism. Its heading/footer/date agree; the prior September4 architecture amendment remains; the revised principle retains local-only identity and adds explicit constant-time validation/origin requirements. The legacy root copy now clearly points to the active constitution. Architecture/technical documentation and environment-variable guidance describe the current bearer/session behavior and remove nonexistent JWT-secret/expiry guidance. The credential-storage paragraph is explicitly left outside this bounded amendment; no broader storage or RBAC acceptance is inferred. Existing historical audit/status evidence is preserved. Using PATCH for this implementation-reconciliation wording is reasonable given the explicit unchanged behavior/security obligations and standing security-fix authorization.

One follow-on planning reference was identified: the079 plan still names constitution3.1.0. The coordinator has agreed to change only that sentence to bind3.1.1 and the September5 authentication reconciliation while stating unchanged behavior/gates. I reviewed and accepted the proposed wording; its exact committed identity and resulting079 scope will be bound separately. This is planning freshness work, not an auth implementation defect or permission to relabel historical planning evidence.

Independent probe/result: `review-jose-removal-ee246102.py` and `review-jose-removal-ee246102-result.json` beside this report. They bind all nine source paths, exact manifest/lock transformations, installed metadata, fresh audit coverage, and hashes of eight retained evidence artifacts. No duplicate tests, audit query, install, build, network credential disclosure, or parent source/state mutation was performed by this reviewer. The combined candidate still requires its new full merge gate, fresh wheel/sdist acceptance, final independent binding/freeze and the separately required CI, development and human obligations.
