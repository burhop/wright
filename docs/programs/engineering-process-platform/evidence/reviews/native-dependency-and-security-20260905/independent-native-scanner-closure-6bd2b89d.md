# Independent bounded native evidence scanner review

**Passed: no P1/P2 findings in the scanner correction** `6bd2b89d5d07e50390307d44ffe5ccd214161243`, tree `7ee8b9a4cae882b9fed930646b2c619ae3969997`. Reviewer `/root/native_candidate_review` authored neither the correction nor its retained writer tests.

The three changed files are `.gitleaks.toml`, new `scripts/test-gitleaks-native-evidence.py`, and the existing `scripts/test-gitleaks-program-status-allowlist.sh`, which invokes the new control after the existing controls. Existing rules, thresholds and allowlists are unchanged. New policy is exactly three AND-constrained entries targeting `generic-api-key`: two exact browser-evidence paths with five literal revision-hash matches each, and one exact source-map evidence path with three literal key/hash matches. Every path and matched key/value regex is anchored at both ends and uses `regexTarget = "match"`. No environment/report credential exemption, arbitrary hex64/token exemption, directory exemption, or file-only exemption was added.

The independent parser/probe verified all13 literal contexts against the actual classified evidence and confirmed old configuration was otherwise unchanged. Source hashes remain the exact candidate60 file hashes already independently recomputed. CAS values remain the ten exact document-revision fields whose deterministic producer and separate authentication boundary were independently inspected. This exception does not permit arbitrary values at these paths. The committed LF configuration SHA-256 is `69e8c7d0923eead407b001d035547fdf48aea1a3cbc9333273615cebbd0fbf00`.

Writer evidence at `D:/repos/wright/.local-run/native-process-milestone/native-audit-status/test-results/gitleaks-native-evidence-d7034776f47948cea37d75e3c8072709` records actual Git-mode Docker controls: zero findings for all13 approved values (including the configuration file itself), then five findings for five adverse contexts. I inspected the retained redacted findings and matched each intended key/path: an adjacent API key in the source-map file, a different token at an allowed browser path, an allowed digest under an API-key field, an allowed digest at an unapproved path, and a `WRIGHT_API_TOKEN` environment entry in the Playwright reporter path. All five are detected as `generic-api-key`; the original tests are preserved.

Those controls did not exercise digest length/prefix mutations. I performed one additional bounded Git-mode Docker scan in my isolated review scratch directory. All four adverse values were detected: an approved token plus one suffix character, an approved token shortened to63 characters, an approved token with one prefix character, and an approved source digest plus one suffix character. The scan reported exactly four expected `generic-api-key` matches across the two intended files. No full-history scan or full gate was repeated. Focused read-only Ruff lint and formatting checks for the new Python control also passed.

The independent control used the installed Gitleaks8.30.1 image by actual image ID `sha256:c00b6bd0aeb3071cbcb79009cb16a60dd9e0a7c60e2be9ab65d25e6bc8abbb7f`, with `--pull never`, `--network none`, and read-only fixture/config mounts. Only a redacted report/log output directory was writable to the container. The initial default sandbox attempt could not access the Docker pipe; the same bounded operation then ran under approved local Docker access. That infrastructure denial is not represented as a scanner success, and no code or policy was changed to obtain the successful check.

Independent proof: `review-native-scanner-controls-6bd2b89d.py` and `review-native-scanner-controls-6bd2b89d-result.json`, completed at `2026-09-05T03:59:51.730904Z`, in this review worktree's `.local-run/`. The actual independent redacted scanner report/log and synthetic fixture repository remain in `.local-run/native-scanner-length-controls-6e726ae83b614bfbb9b7bf139e134222/`.

This closes only the13-hash exception and detection-control review. The separate environment-capture removal was independently verified at80eaef9c across8,840 reachable blobs; this scanner patch does not retroactively sanitize old private history. The parent must run the full publishable-history scanners after integration, retain the projected evidence map, and complete the fresh required gate/freeze before publication or a new entire-candidate technical approval. No human-study, development-deployment, CI or whole-feature completion credit is granted here.
