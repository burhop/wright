# Independent public evidence and attachment-index closure

**Result: PASS. The post-freeze attachment-index finding is closed.** Reviewer: `/root/native_candidate_review`, independent of the metadata and implementation writers. Reviewed metadata commit `615538f66fa5c141b897b7241d8e37e4b5d0a5b2`, tree `8fca4ac82632a4c2242cc14cc15733c5745a79ff`, including the attachments retained at `c401712b5f6f30dc6c0685bbd4ae3ddc5cabdeba`. This is a bounded metadata review; it does not replace the existing frozen-candidate technical record.

The earlier generated index named original JSON filenames rather than their retained `.json.txt` paths, omitted two logs from committed files, and did not distinguish original raw bytes from Git-normalized text bytes. The explicit `committed-attachment-map.json.txt` now maps every one of the 11 selected originals to a committed path, commit, SHA-256, size and declared transformation. Both Docker logs are retained as `.log.txt`. The original selected index and external Docker manifest continue to identify original source files; `attachment-format.md` explains that distinction.

The independent read-only probe `review-metadata-615538f6.py` passed against actual local original files and Git blobs. All 11 original and retained digest/size pairs match their records: four files are unchanged, five differ only by CRLF/LF normalization, and two are explicitly labeled public review projections. No mapped attachment is missing. The files at each declared retained commit match the reviewed metadata head.

The two public browser projections replace exactly these five quoted string values with the manifest's explicit omission marker:

- `actual-native-example-runs.json.txt`: `/0/snapshot/token`, `/1/snapshot/token`.
- `actual-native-runs.json.txt`: `/initial/snapshot/token`, `/failed/snapshot/token`, `/corrected/snapshot/token`.

The probe reconstructed each projection from the original raw bytes by replacing only those five strings and obtained byte-identical committed output. It also compared parsed JSON after applying only those replacements. Every other value and byte remains unchanged, including semantic digests, revision numbers, run states, bindings, inputs/outputs, events, traces and artifact evidence. Both original browser captures and their additional retained local copies still match the original hashes and bytes. The public files are correctly described as derived review projections, not unchanged API responses or complete request-replay inputs.

Source inspection confirms these `snapshot.token` fields are document revision CAS fingerprints: the repository hashes workspace identity and the saved envelope; authorization remains a separate service check. Removing these values from a labeled public evidence projection does not alter product authorization or code. It also does not establish that unrelated fields or dependencies are free of security issues.

I inspected the retained redacted scanner reports and terminal projection log: the original bounded directory scan recorded five `generic-api-key` findings; the final projection report is empty and its log records exit 0. The reported scan used the already-cached pinned Gitleaks v8.30.1 image, existing configuration, read-only mounts, `--pull never`, `--network none` and full redaction. The committed scanner configuration is unchanged from frozen code60. No new scan was run by this reviewer. The final scan covers the two projections plus their manifest and note; it is not a scan of all reachable Git history, all metadata or a new dependency audit. The notes preserve that scope and do not claim otherwise.

All 37 paths added between frozen state `b85ef464818251972903d6875db72e4198825e36` and reviewed metadata 615538f6 are under program review evidence. The frozen program-state bytes are unchanged at 615538f6. The typed technical review is byte-identical to my original, with SHA-256 `e427fdafc85517ef5d7eb60160594e1905d87799a2d16c3980c1fd85c6eb6307`, and still names frozen code `60ef8672f1f61c2f4942e618638ec8901e9aa9a0`. I also confirmed 615538f6 is an ancestor of the resumed parent `7c0d3ef3358b62493ac12e1bd823f901983e44f0`; its typed review and projection/attachment manifests are unchanged there, and all post-freeze paths remain under the program directory. No production, scanner-policy or full-gate source changed for this transformation.

No new P1/P2 issue was established in this metadata scope. This closes the evidence-index repair condition recorded in the final technical review; it grants no additional runtime, human-study, CI, deployment, benchmark or publication result. Later task-checkbox changes inside 079 require the separately scoped planning identity refresh already noted in the planning review.
