# Native milestone scope and prospective lifecycle

The September 4 user goal authorizes bounded implementation and prospective policy amendments. AUTH-EPP-N01-2026-001 preserves that instruction verbatim and distinguishes scope authorization from approval of unseen bytes. Its schema and validator apply only to EPP-N01 from revision 93. Historical exact-approval records and their validation remain unchanged.

F02 is integrated; deployment verification is pending. EPP-N01 depends on the completed browser/platform foundation EPP-F01B because its native language and runtime are independent of the immutable F02 sample. Selecting this milestone does not grant F02 deployment credit. N01 records that remaining check explicitly.

One current worktree lease controls implementation; close it before independent candidate verification and CI. Scope authorization covers the user's named bounded actions, with all existing push/merge gates. Main, publication, paid/proprietary/credentialed effects and physical actuation remain excluded. Actual human review remains required evidence.

The native language is the common authority for AI/programmatic clients and canvas/runtime. General AI authoring, automatic cycles, full benchmark qualification and Rivet migration/retirement remain separately tracked. Native delivery must not mark those obligations complete.
