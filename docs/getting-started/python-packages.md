# Python Packages

The public-alpha Python package is:

```bash
pip install wright-engineering
wright doctor
```

This package is a helper and discovery surface. Docker remains the primary end-user path for running the Wright appliance.

Component packages such as `wright-core` and `wright-tool-registry` are workspace-local for alpha and are not promised as public PyPI installs.
