# Selected baseline

`baseline.json` is the sole candidate record for this spike. It pins the upstream commit and published package versions; changing either requires a new spike or an explicit update decision.
