# Production Image Dependency Inventory

Feature 047 validates one `linux/amd64` appliance. Multi-architecture indexes are not a support claim.

| Input                          | Pin                                                                                                                                                                                                                                   | Verification/update rule                                                                                                                                                                                                                                                                                               |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Node web builder               | `node:26-slim@sha256:4ebb5ac...` (Node 26.7.0)                                                                                                                                                                                        | Node current line; resolve the official index digest and rerun frontend plus exact-image smoke before updating.                                                                                                                                                                                                        |
| Python/Hermes base             | `python:3.13-slim@sha256:ffb752e...` (Python 3.13.15)                                                                                                                                                                                 | Official image index; Hermes 0.19.0 supports Python 3.11-3.13; validate amd64 manifest, Python runtime, API/Hermes smoke, scan, and licenses.                                                                                                                                                                          |
| Final runtime base             | `debian:trixie-slim@sha256:3a39a05...`                                                                                                                                                                                                | Copy only the current Python/Hermes runtime from the builder so the final candidate does not retain superseded Python package records from the intermediate base layers.                                                                                                                                               |
| Hermes Agent                   | `0.19.0`                                                                                                                                                                                                                              | Install into `/opt/hermes/.venv`; require the standard Wright plugin lifecycle, direct gateway health, and final `uv pip check`.                                                                                                                                                                                       |
| uv binary                      | `ghcr.io/astral-sh/uv:0.9.26@sha256:9a23023...`                                                                                                                                                                                       | Official Astral image index; copy only `/uv`; verify version in image inventory.                                                                                                                                                                                                                                       |
| micromamba                     | `2.5.0`, SHA-256 `cec496f2...103b6`                                                                                                                                                                                                   | Download the versioned linux-64 archive and fail the build on checksum or architecture mismatch.                                                                                                                                                                                                                       |
| Python workspace               | committed `uv.lock`                                                                                                                                                                                                                   | `uv sync` must use the lock and dependency audit must pass.                                                                                                                                                                                                                                                            |
| Hermes security reconciliation | `cryptography==50.0.0`, `Pillow==12.3.0`                                                                                                                                                                                              | Hermes 0.19.0 metadata exactly pins vulnerable `cryptography==46.0.7` and `Pillow==12.2.0`. Run raw `uv pip check`, then accept only those two exact version conflicts with `scripts/reconcile_hermes_pip_check.py`; any other mismatch fails. Remove the reconciliation when Hermes publishes compatible secure pins. |
| Hermes runtime patches         | exact versions in `docker/Dockerfile`                                                                                                                                                                                                 | Changes require `uv pip check` reconciliation, blocking vulnerability-policy, gateway smoke, and public plugin lifecycle evidence.                                                                                                                                                                                     |
| Container scan patches         | explicit util-linux security family, npm 12.0.2 nested overrides, pip vendor msgpack 1.2.1 plus removal of its unsupported legacy pkg_resources module (and setuptools 78.1.1 inventory), system msgpack 1.2.1, and setuptools 84.0.0 | Install only named Trivy-fixed packages after apt-get update; update both the supported vendored runtime code and its SBOM where an upstream tool still records a vulnerable version. Do not use apt-get upgrade; rebuild the exact candidate and enforce the blocking release vulnerability policy.                   |

The Dockerfile contains no `apt-get upgrade`, moving `latest` input, or unchecked binary download. Release workflows build the candidate once, store its digest, and direct all smoke, scan, SBOM, provenance, tag promotion, and optional mirror verification at that digest.

Rebuild comparison is diagnostic: repeat builds may differ because upstream package repositories and timestamps are not yet fully hermetic. Release identity is established by testing and promoting the first immutable digest, never by rebuilding and assuming equality.
