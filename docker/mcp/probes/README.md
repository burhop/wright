# MCP Bundle Probes

Probe files describe how `scripts/docker-mcp-smoke-test.sh` should interpret
each bundle entry. They are intentionally declarative so source/license review,
Docker installation, direct MCP health checks, and Playwright prompts can evolve
without changing the bundle manifest schema.

