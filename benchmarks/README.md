# Governed benchmark workspace

This directory is intentionally empty of benchmark case content.

The current benchmark preflight is blocked by unresolved human-owned oracle,
holdout, population, threshold-artifact, and license/provenance decisions plus
unfinished roadmap dependencies. `scripts/recovery/benchmark_preflight.py`
must report `PASS` before any case manifest, fixture, oracle, or output is
generated here. Until then, the governed result remains exactly `0/100`.

Do not place blind-holdout definitions, assets, or oracles in this repository.
