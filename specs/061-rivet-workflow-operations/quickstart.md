# Quickstart

Enable workflow, runner, and operations flags in a disposable environment.
Create a workflow, open the Rivet Workflows workspace tab, approve its current
revision, then run it and view/cancel the bounded event history. Save a new
revision and confirm Run stays blocked until the new revision is approved.
Disable the operations flag and confirm the tab/API report unavailable without
affecting ordinary workspace functions.
