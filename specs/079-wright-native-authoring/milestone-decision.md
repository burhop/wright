# Decision: Native End-to-End Milestone

**Date:** 2026-09-04. **Authority:** the user's submitted `/goal Implement Wright’s first complete native engineering-process milestone` in this task. This is a scope record, not a signature or approval of future exact implementation bytes.

The milestone supersedes the prospective manual-only scope of the September 2 proposal, preserved at `6daeb214` and in the original user worktree. Execution and dashboard work are now expressly requested. Historic evidence retains its original meaning.

Standing authorization covers necessary bounded architecture/roadmap/dashboard changes, routine decisions, reviewed/pinned dependencies, isolated worktrees, commits, feature pushes, PRs and dev merges after gates/independent review. Direct dev pushes, main/release, paid services, new credentials, proprietary installation and physical actuation remain outside scope.

## Decisions

- The versioned Wright process language is the official semantic authority for AI clients, canvas/Inspector, readable text, API, CLI and runtime. No new native path serializes through Rivet. Immutable F02 stays unchanged. [Language authority and replacement contract](contracts/language-authority.md).
- Structured editing and read-only source precede an editable DSL and autonomous AI authoring experience. The shared schema/API contract supports programmatic clients now. Initial catalog contains sources, registered operations, checks, artifact output and a subsequently verified exact-bound MCP task.
- Quantities use bounded decimal strings and explicit units. No implicit type/unit guessing.
- Forward dependencies form a DAG. Failed checks identify corrections; edits and reruns create a fresh run linked to the old immutable evidence. Automatic cycles and human-approval runtime steps are deferred.
- Narrow the earlier seven creation groups to implemented functionality. No fabricated Run/AI/tool/output states.
- Explicit SQLite transactions, state CAS and idempotency protect persisted work. Additive schema migration still requires true old-reader rejection/backup/forward recovery evidence.
- Product, benchmark, commercial and release obligations remain independent. Development examples receive no process-100 qualification credit.

## Prospective governance

Scope clarification, September 4: the user reiterated replacing Rivet and making the process language official for both AI and the canvas. This is adopted explicitly in FR-001/FR-002 and the language-authority contract. Native milestone completion and full Rivet migration/retirement are separate deliverables. Acceptance coverage is strengthened within T002/T003/T004/T006/T011–T014/T016/T021/T023/T032; the 32 task IDs and denominator are unchanged. No additional completed work or AI-generated evidence is claimed.

The user authorizes bounded decisions and asks to reconcile repeated prospective approval rules. Complete concrete planning and independent consistency review before implementation, then proceed under this scope. Never manufacture human approval of an unseen exact subject. Material expansion outside this milestone requires a concrete user decision. Historical states/transitions/corrections remain unchanged; new evidence is appended.

## Baseline

Dev observed September 4: `7404a549ae244cc05d89e062c60276e8862f53c9`. PR #117 and Docker flag PR #120 merged August 31. Post-merge CI is not proof of browser deployment; verify missing deployment evidence now without backdating.

Architecture/dashboard investigations found transaction, terminal-state, previous-reader, hardcoded-feature, stale-status and evidence-age defects to address. They are planning reviews, not passed implementation evidence. Independent human usability evidence is pending; a tester-arrangement question was sent to the user.
