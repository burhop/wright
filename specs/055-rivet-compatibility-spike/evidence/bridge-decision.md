# Bridge decision

Use Rivet External Call only behind a Wright-owned allowlisted bridge. The bridge must carry workspace and principal context, use approved Wright operations, enforce cancellation and resource limits, and prohibit direct plugin/MCP access from graph content.
