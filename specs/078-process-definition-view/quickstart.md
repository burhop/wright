# EPP-F02 Human-Repeatable Demonstration

1. On the exact approved candidate, run `uv run --extra runtime --extra engineering-models python -m pytest packages/tool_registry/tests/test_process_definition.py apps/api/tests/test_process_definition_api.py tests/packaging/test_wheel_contents.py tests/native_runtime/test_process_definition_lifecycle.py tests/e2e/test_process_definition.py --import-mode=importlib`, then `npm --prefix apps/web run test -- --run ProcessDefinition`, and finally `npx playwright test tests/ui-integration/process-definition.spec.ts --project=chromium`. Use an unused `WRIGHT_PLAYWRIGHT_PORT`, keep Playwright's managed Vite server non-reusable, and record each exact total; any missing named target or nonzero result fails this step.
2. From the repository root, start the API in one PowerShell terminal with `$env:WRIGHT_DATA_ROOT = ".local-run/epp-f02-demo"`, `$env:WRIGHT_AUTH_MODE = "compat"`, `$env:WRIGHT_API_MCP_AUTOSTART = "0"`, then `uv run --extra runtime uvicorn api.main:app --host 127.0.0.1 --port 28002`. In a second terminal set `$env:VITE_WRIGHT_PROCESS_DEFINITION_VIEW = "1"` and `$env:WRIGHT_WEB_API_PROXY_TARGET = "http://127.0.0.1:28002"`, then run `npm run dev --workspace=apps/web -- --host 127.0.0.1 --port 25182`. Open `http://127.0.0.1:25182/processes/product-definition-v1`. These isolated ports and data root are the frozen local demonstration boundary; no model, MCP server, or external service is required.
3. Record process ID, revision, schema version, and content identity.
4. Trace one input through an action and gate to an expected artifact in text.
5. Locate the same semantic IDs in the diagram.
6. Complete keyboard-only navigation at 320 CSS pixels and 200% zoom.
7. Stop the API, create only `.local-run/epp-f02-demo/process-definitions/product-definition-v1.json` from the UTF-8-without-BOM `exact_text` of fixture `invalid-truncated-json` in `contracts/recovery-fixtures.json`, and verify SHA-256 `7dcddf72598670f17b137e82dd73c657c7a825e92088f30ac3361c86dc014ad9` before and after the check. Restart the same API command and confirm HTTP 422, `PROCESS_DEFINITION_INVALID`, `replace_validated_definition`, no partial process content, and no mutation of the fixture bytes.
8. Stop the web server, set `$env:VITE_WRIGHT_PROCESS_DEFINITION_VIEW = "0"`, restart the same web command, and confirm the navigation entry is absent and the direct route falls back safely while the selected workspace/Rivet non-interference smoke remains green. Remove only `.local-run/epp-f02-demo` after the demonstration.
9. Report exact commands, totals, commit/tree, outputs, limitations, and rollback point.

Stop at the first failed assertion, missing artifact, identity mismatch, unavailable command, unexpected mutation, or absent expected state in any step; record the exact failing step and evidence instead of substituting, inferring, or continuing the demonstration.

The page explains only. It does not edit, Apply, execute, persist, invoke tools, produce artifacts, or qualify benchmarks; governed qualification remains `0/100`.

## Frozen local candidate

The completed T001–T019 product subject is commit `e9bfaea0cd6f6f0671966ae12dcabd7c3b6d256f`, tree `de15cf16fd6a33a0b94221ae751e5d6f8a8ff351`, and program tree `5713e23a354ec6fde2c37d23910e566c1af7754b`. It is based on `dev` commit `e9dd5c38026d0b1b5b165ee803fe082068ecc128`. The self-contained Git bundle is 44,120,010 bytes with SHA-256 `d2432308c9fbc81367055eaa77242ff13bffcbcc4bb47b8597b78e81d8af0219`.

Windows 11 passed the exact validator with zero blockers, the 73-test Python quickstart matrix, 46 focused browser tests, four Chromium acceptance/accessibility journeys, TypeScript, the complete isolated success/invalid/disabled demonstration, contamination and prototype-diff audits, and Wright's prescribed fast push gate. That gate included API 417 with one skip, tool registry 395, broad regression 405 with 38 skips, E2E 1, native 132 with 10 skips, native lifecycle 1, packaging 2, program control 312 with one skip, the focused release/security suites, 84 changed web tests, the production build, Chromium 4, and strict docs.

GB10 independently verified the SHA-matched bundle on Linux aarch64 with Python 3.13.12. The distinct package/E2E slice passed 3/3 in 11.58 seconds, the exact validator passed with zero blockers, both the primary clone and disposable checkout remained clean, and the contamination audit found zero feature-term hits and zero exact prototype blob reuse. A separate Windows verifier then passed 46/46 Vitest and 4/4 Chromium on a fresh managed server between `2026-08-31T03:03:38.1400168Z` and `2026-08-31T03:05:45.8542233Z`, with no P0/P1/P2 and unchanged candidate identity. Its first sandboxed browser launch was blocked before assertions by host `spawn EPERM`; the authorized unchanged rerun passed.

The demonstration returned packaged-fallback HTTP 200 with the exact ETag and bodyless conditional 304 for `product-definition-v1` revision 1, schema `1.0.0`, content SHA-256 `4617a8a6424b7c15712cd951c0b97a8c4ec5e77e29633e46e105d30ec65d5883`, and source SHA-256 `6a02f71e35f9c3d9a3184509ddeab2df251cff454b6d6ce66d7244d015eefdef`. The exact invalid fixture remained byte-identical while returning HTTP 422, `PROCESS_DEFINITION_INVALID`, `replace_validated_definition`, and no partial content. With the feature disabled, real Chromium at 320 × 640 showed no process navigation and recovered the direct route through the existing bounded NotFound/dashboard path.

Rollback keeps `VITE_WRIGHT_PROCESS_DEFINITION_VIEW` disabled to remove navigation and route exposure. Reverting the feature PR removes the packaged definition, read-only API, and page; the clean baseline is the `dev` commit above. No benchmark case, dependency, release, publication, production, or customer-system action is part of this candidate. `process_100_id` remains null and every governed benchmark stage remains `0/100`.
