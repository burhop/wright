# EPP-F02 Human-Repeatable Demonstration

1. On the exact approved candidate, run `uv run pytest packages/tool_registry/tests/test_process_definition.py apps/api/tests/test_process_definition_api.py tests/packaging/test_wheel_contents.py tests/native_runtime/test_process_definition_lifecycle.py tests/e2e/test_process_definition.py --import-mode=importlib`, then `npm --prefix apps/web run test -- --run ProcessDefinition`, and finally `npx playwright test tests/ui-integration/process-definition.spec.ts --project=chromium`. Use an unused `WRIGHT_PLAYWRIGHT_PORT`, keep Playwright's managed Vite server non-reusable, and record each exact total; any missing named target or nonzero result fails this step.
2. From the repository root, start the API in one PowerShell terminal with `$env:WRIGHT_DATA_ROOT = ".local-run/epp-f02-demo"`, `$env:WRIGHT_AUTH_MODE = "compat"`, `$env:WRIGHT_API_MCP_AUTOSTART = "0"`, then `uv run uvicorn api.main:app --host 127.0.0.1 --port 28002`. In a second terminal set `$env:VITE_WRIGHT_PROCESS_DEFINITION_VIEW = "1"` and `$env:WRIGHT_WEB_API_PROXY_TARGET = "http://127.0.0.1:28002"`, then run `npm run dev --workspace=apps/web -- --host 127.0.0.1 --port 25182`. Open `http://127.0.0.1:25182/processes/product-definition-v1`. These isolated ports and data root are the frozen local demonstration boundary; no model, MCP server, or external service is required.
3. Record process ID, revision, schema version, and content identity.
4. Trace one input through an action and gate to an expected artifact in text.
5. Locate the same semantic IDs in the diagram.
6. Complete keyboard-only navigation at 320 CSS pixels and 200% zoom.
7. Stop the API, create only `.local-run/epp-f02-demo/process-definitions/product-definition-v1.json` from the UTF-8-without-BOM `exact_text` of fixture `invalid-truncated-json` in `contracts/recovery-fixtures.json`, and verify SHA-256 `7dcddf72598670f17b137e82dd73c657c7a825e92088f30ac3361c86dc014ad9` before and after the check. Restart the same API command and confirm HTTP 422, `PROCESS_DEFINITION_INVALID`, `replace_validated_definition`, no partial process content, and no mutation of the fixture bytes.
8. Stop the web server, set `$env:VITE_WRIGHT_PROCESS_DEFINITION_VIEW = "0"`, restart the same web command, and confirm the navigation entry is absent and the direct route falls back safely while the selected workspace/Rivet non-interference smoke remains green. Remove only `.local-run/epp-f02-demo` after the demonstration.
9. Report exact commands, totals, commit/tree, outputs, limitations, and rollback point.

Stop at the first failed assertion, missing artifact, identity mismatch, unavailable command, unexpected mutation, or absent expected state in any step; record the exact failing step and evidence instead of substituting, inferring, or continuing the demonstration.

The page explains only. It does not edit, Apply, execute, persist, invoke tools, produce artifacts, or qualify benchmarks; governed qualification remains `0/100`.
