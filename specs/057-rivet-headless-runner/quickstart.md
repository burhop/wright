# Quickstart

Enable the test runner flag, save a synthetic workflow, start it, observe events, cancel it, and verify no child process remains. Repeat with Node absent: Wright stays healthy and returns unavailable.
