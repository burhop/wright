# Proposed `/goal` After Plan Approval

```text
/goal Implement the approved Engineering Workflow Templates feature from specs/081-engineering-workflow-templates/plan.md on codex/081-engineering-workflow-templates, following constitution v3.1.1 and docs/contributing/workflow-ui-integration.md. First run /speckit-tasks and /speckit-analyze and resolve material findings. Deliver the UI milestone first: exactly ten truthful engineering templates under Start from template in the established workspace Workflows editor, fresh atomic editable instances, readiness/prerequisite details, and served-workspace browser verification preserving all reviewed authoring behaviors. Then implement durable digest-bound approval/resume and make the first three workflows genuinely work: image and scale to verified mesh, supports/slice and explicitly approved Bambu P1S transfer; manufacturer-sourced Raspberry Pi dimensions to reviewed design document, qualified AgentCAD model, verified matching CFD domain and solver-field comparison; and the recovered sheet-metal design/check/two-revision/export/DXF/supplier-preview/user-handoff chain with no order or payment. Follow the clean-container MCP qualification process, never promote fixtures or discovered servers as live evidence, and add independent actual-output assertions and a negative/recovery case for every working workflow. After the first three pass, qualify the remaining seven in the contract order. Generate only local, redacted, lineage-bound social capture packages; do not post. Pause at the constitution's required review boundaries and before printer transfer, supplier upload/handoff, paid/credentialed external work, push, merge, release, purchase, payment, production release, or publication unless that exact action has already received explicit user authorization.
```
