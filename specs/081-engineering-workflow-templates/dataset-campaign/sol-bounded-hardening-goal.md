# Bounded Sol reliability goal

Start a fresh Codex task in `D:\repos\wright` and explicitly select Sol before
pasting the prompt. Do not paste the previous conversation. This file does not
start a goal or select a model. The limits below bound work attempts, not billed
tokens; use any available account spending controls separately.

```text
/goal Complete one bounded reliability-hardening pass for Wright feature 081, then qualify and attempt the remaining Raspberry Pi enclosure/CFD pilot. Deliver tested improvements, the real pilot outcome, an updated dashboard and a compact handoff. This goal does not require completing all 30 scenarios.

Work in D:\repos\wright. Follow AGENTS.md, the constitution and the existing feature-081 Spec Kit plan. Read dataset-campaign/efficiency-review-2026-09-14.md under specs/081-engineering-workflow-templates, then only the relevant source and evidence. Preserve unrelated changes and successful runs. Local implementation, focused tests and necessary owned-service restarts are authorized; no routine confirmation pauses.

Use the explicitly selected Sol model. Verify the separately configured Wright inference model before any pilot; never silently use Astra or another model. Work as one agent. Do not resume old agent tasks, start subagents, or import old conversation histories. Solid Edge coordination is already recorded in dataset-campaign/solid-edge-agent-handoff.md; fundamental provider work remains a separate assignment.

First inspect compact ledger/diagnostic summaries and actual service health. The last recorded baseline was 30/30/20/0 historically, but 19 current-revision completions because PCB02's input fingerprint changed. Reconcile that discrepancy without reverting inputs to obtain credit. Disk cleanup is addressed; retain storage preflight.

Implement the smallest reusable fixes in this order, reusing existing components:
1. Dashboard: serve persisted state promptly with reconciliation status; move expensive audits off the request path and avoid rehashing unchanged output trees on every scan. Preserve explicit full audits, change invalidation and honest freshness indicators.
2. Model/tool protocol: qualify malformed/null decisions and completed-operation handling in a tiny disposable workflow. Use explicit terminal-operation receipts for routine successful saves/exports. Preserve replay guards and unknown-mutation reconciliation; never fabricate completion.
3. Stage contracts/context: serialize routine schemas and native measurements in code; validate filenames, arguments and required resources before expensive execution. Pass compact typed summaries and artifact references instead of full reports between stages. Keep model responsibility for human inputs, images and design decisions.
4. Persist bounded retry episodes and available model usage. Fresh prompts, grants or attempt IDs must not reset retry limits. Record missing telemetry as unknown. Keep requirements distinct from permitted design choices; address only design-authority defects encountered in this pilot.

Prove affected boundaries with retained failing evidence in disposable diagnostics, then run Pi03 through the normal complete canonical workflow. Permit at most two full pilot attempts, with a demonstrated material fix required between them. Diagnostic reuse never earns full-run credit. Preserve every required stage, approval, same-run artifact identity and owned-application cleanup.

Efficiency limits: at most six fix/test cycles overall and two safe corrections per defect. Use focused tests plus required repository checks; rerun broader suites only for a demonstrated dependency impact. Keep tool output compact. Let ordinary processes wait for native work and update the dashboard; do not repeatedly ask the model to inspect unchanged state. Continue independent fixes when a dependency is blocked.

Keep http://127.0.0.1:8771/?view=recovery available. Update the four historical series on real changes and expose current-revision completion, failure reasons and cleanup status separately. Keep content validation disabled and valid_data=0. Physical printer/vendor actions and push/merge/release are outside this goal.

Finish this bounded pass when the fixes and pilot evaluation conclude or an attempt limit is reached. At a limit, launch no further work: preserve evidence and a resumable checkpoint. Update the relevant Spec Kit task statuses and save a handoff of at most 600 words listing changes, tests, actual models/usage, pilot outcome, unresolved defects and exactly one recommended next task. Report incomplete work explicitly; never claim the overall campaign is complete. Do not start another goal automatically.
```
