# Fresh cases after selected AgentCAD bootstrap installation

September 12, 2026. The corrected demo registration was verified after API
startup reconciliation: all eleven AgentCAD tool/schema identities match the
qualified bootstrap installation and differ from their previous values.
The root-owned discovery evidence is
`.local-run/feature-081-live/campaign-execution/post-restart-tool-discovery.json`.

Nine fresh cases were then created using ordinary template instance/save APIs
and the trusted integration enrollment command. Each retains every original
API-created task ID, with its family's existing extra review/engineering
stages. Native `agentcad init` ran sequentially in nine new disposable projects;
no model/kernel, solver or workflow execution was dispatched during staging.

| Manifest under `.local-run/feature-081-live/campaign-execution/` | Cases | Stages per case | Exact tool pins | Input file counts | Declared tool-created file counts |
| --- | --- | --- | --- | --- | --- |
| `heat-attempt-004.json` | 3 | 7 | 7 | 10 / 10 / 11 | 15 / 20 / 20 |
| `jig-attempt-004.json` | 3 | 7 | 7 | 10 / 10 / 10 | 7 / 7 / 7 |
| `bracket-attempt-002.json` | 3 | 11 | 10 | 11 / 11 / 11 | 42 / 42 / 42 |

`agentcad-refresh-nine-staging-verification.json` records the accepted source
and policy digests, original stage identities, current tool pins, unchanged
staged input hashes, native initialization success and confined output paths.
The aggregate eleven-tool pin digest is
`3a143c486a147380eea0d5e1f82bae394e0437c41dd5e2a7388c8ca09919ae54`.
Heat solver paths explicitly use the demo's `/campaign-workspace` mount.
`agentcad-refresh-nine-native-initialization.json` links the actual native
receipts. Project initialization does not constitute a completed workflow or
produce campaign completion credit.

Automatic approval review rejected the initial combined staging/revocation
command before execution because it considered the existing policy mutations
insufficiently authorized. The safer staging-only operation was approved and
completed. No old grant was revoked, and revocation capability was removed from
the active management script. The obsolete grants already fail current exact
tool-identity checks.

`agentcad-refresh-nine-stale-grants-readonly.json` confirms the nine superseded
unstarted grants remain unrevoked, their sources/inputs are unchanged, none is
associated with a durable run, and their AgentCAD tool pins differ from the new
registration. Failed prior runs and every old attempt remain intact. The
management scripts passed Ruff; these observations establish staging readiness,
not engineering correctness or full-process completion.
