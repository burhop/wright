# Ready-to-paste Sol recovery goal

Updated after the September 12 approved recovery review and native application
lifecycle request (September 13 UTC). Select **Sol** in the app before submitting.
Use the actual supported model ID exposed by the current installation; do not
invent an ID from the phrase "GPT-6 Sol." Goal text does not change the model
selection. Verify the separately configured Wright workflow inference model too.

This prompt starts no process merely by being saved. It supersedes the earlier
broad-batch prompt. The latest observed campaign baseline is 30/29/9/0;
`execution-state.md` and the durable ledger govern current state.

~~~text
/goal Complete the existing Wright feature-081 engineering dataset campaign to 30 complete human input packs, 30 accepted dataset/workflow pairs, 30 full workflows with their expected nonempty output files, and 0 content-validated results. Follow specs/081-engineering-workflow-templates/dataset-campaign/plan.md, focused-recovery.md, tasks.md (DC012–DC025), contracts/native-application-lifecycle.md, and execution-state.md. Stay on codex/081-engineering-workflow-templates, follow AGENTS.md and the constitution, and preserve unrelated work. This authorizes ordinary local planning, implementation, dependency setup, tests and fixes without routine approval pauses.

Use the selected Sol model efficiently for this task and supported Wright workflow inference. Check and record actual model identities; do not silently switch models or invent pricing. Read the compact current checkpoint first, then only relevant evidence. Use bounded independent subtasks when useful, compact stage context, direct generic calls for fixed operations, and focused regression tests. Keep AI responsible for interpreting the human images, requirements and engineering design.

Start by reconciling the actual ledger, workers, application sessions and historical owner-missing run. Preserve all nine previously completed current-revision cases if still valid. Do not blindly launch the old Batch021 queue. Implement the minimum ownership/lease/cleanup path for affected Solid Edge and Blender resources before their next native mutation. Distinguish campaign-owned processes and documents from borrowed user sessions using PID, creation time, executable and native session/endpoint identity. Reuse only healthy clean idle instances; close owned documents and gracefully shut down owned applications after the configured idle period and on campaign termination. Handle success, failure, cancellation, timeout and restart with durable cleanup/reconciliation receipts. Quarantine ambiguous or hung resources while independent work continues. Never kill by application name, close unrelated/unsaved user work, or treat a transport timeout as proof that a native operation stopped. Forced cleanup is permitted only under the documented exact-ownership/data-preservation conditions and platform permissions. Retain Blender safe mode; the lifecycle amendment does not answer the outstanding script-guard choice.

For each failing family: inspect its retained failing stage, reproduce the failure in an owned isolated diagnostic project under the pinned software version, make the smallest reusable fix, and pass that specific probe before a full rerun. Prioritize ready Pi and sheet-metal pilots, and printing when its actual prerequisites are resolved. Continue ready independent follow-ons around blocked resources. Get one full representative workflow passing before expanding that binding to its other two datasets. Use at most two safe local source/recipe corrections per stage, retaining versions and diagnostics; respect any stricter existing rework bound. Preserve approval/input/source/tool authority, invalidate dependent results after changes, and never replay an unknown mutation. Quarantine repeated family failures until a material fix is proven. Add resource-aware scheduling incrementally; run a safe pilot serially without waiting for a generalized scheduler. Allow at most two concurrent execution lanes only with proven isolation.

Use normal canonical .workflow.wflow execution, existing workspace APIs, GatewayService and immutable attempt evidence. Actual CAD, meshes, supports, slices, solver data and reports must be generated from the supplied human inputs. Preserve all required graph stages. Diagnostic fixtures and reused development evidence earn no completion credit. Only explicitly enrolled external handoffs may be simulated; test-auto approval is limited to local reviews and those test destinations. Keep real printer/vendor writes, orders, purchases, publication and push/merge/release out of scope.

Keep http://127.0.0.1:8771 available and preserve its four historical counter definitions. Add last stage, failure reason, actual attempts, fix/queue age, last completion time, stage duration, available token usage and native application/lease/cleanup diagnostics. Unknown usage stays unknown. Count completion only from a successful full canonical run with same-run nonempty expected artifacts; keep valid_data exactly 0 and Not measured. Do not add a new content-correctness or certification program.

Retain inputs under tests/datasets/engineering-workflows/scenarios and outputs under artifacts/engineering-workflow-datasets/output/<scenario>/<attempt>/ with run, artifact, source/input/tool/model and approval lineage. Preserve historical attempts, diagnose local blockers, and continue useful authorized work across compactions. Update execution-state.md after material changes with compact evidence, actual process identities and precise next actions. Revisit external blockers only when their state changes; never spin on identical errors or manufacture progress.

Before declaring completion, verify 30/30/30/0, all served flagship journeys and output links, restart/approval continuation, and three create/run/close lifecycle cycles for each native application with user-work preservation. Reconcile and close owned idle native applications and publish cleanup receipts while keeping the requested Wright/Hermes/dashboard services available. If only external blockers remain, preserve resumable state and state the exact missing decision/access; do not label partial completion as achieved.
~~~
