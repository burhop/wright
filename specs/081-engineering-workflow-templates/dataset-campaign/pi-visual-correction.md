# Pi reference observation correction

Attempt 002 remains immutable. Its actual manufacturer retrieval and design
review succeeded, then CAD-source authoring returned TASK_BLOCKED: extracted PDF
text had no dimension-leader associations and the design document consequently
prohibited mounting/cut-out geometry. No CAD-source file or native CAD step ran.
The HTTP422 was the task failure surfaced by resume, not stale tool authority.

The future preparer is `scripts/prepare-pi-visual-dataset-campaign.py`. It retains
the original four API task identities and all eight existing bound stages, the same human
inputs, sourced design review, actual AgentCAD CAD and actual native OpenFOAM
comparison. It uses a separate versioned reference server; no original operation
source or attempt002 grant is changed.
It adds a ninth stage using the enrolled scoped `inspect_file` capability to
record actual CAD file hashes and read both CFD contracts before preparation.
Original CSV/TSV text is included verbatim in the design-stage context as well as
retained in its original uploaded file; AI-only design stages cannot otherwise
open a merely staged CSV.

`pi_reference_visual_mcp.py` depends on the exact original reference source.
It retrieves the same three primary Pi PDFs plus the candidate insert's
[primary product page](https://www.ruthex.de/products/ruthex-gewindeeinsatz-m2-5-70-stuck-rx-m2-5x5-7-messing-gewindebuchsen),
its linked
[dimensional table](https://www.ruthex.de/cdn/shop/files/1Tabelle.PT05_eb2a30fa-5c34-46f8-b557-a6c432354560_800x.jpg?v=1748341911)
and
[installation illustration](https://www.ruthex.de/cdn/shop/files/2Beispielbild_89e9a32b-7812-4470-b706-f2e5af5770ef_800x.jpg?v=1748341911).
The model must read the actual table/diagram before selecting the candidate;
the operation supplies no operator-extracted dimensional table or rating.

Three confined tools retrieve these exact sources, read at most4000characters,
or return actual source pixels as native MCP TextContent plus ImageContent.
Each view records source URL/hash, page, fixed full/quadrant crop, image dimensions,
PNG hash and workspace-relative derivative path. A view has at most2200pixels on
its longest edge and3MiB raw PNG (4MiB base64). Unknown documents, URLs, redirects,
source changes, cross-attempt paths and changed source bytes fail closed.

The workflow research stage observes source images itself. The generic runtime
must forward native MCP images to model vision; serializing base64 into textual
tool messages cannot satisfy that prerequisite. The generic patch is maintained
separately in workflow_mcp_execution.py and the Hermes bridge. Its task-wide
image count/byte budgets also apply.

The new concise CFD contract excludes qualification history. Separate authored
prototype guidance distinguishes designer-selected seams, feet, fastener layout
and access clearances from sourced component facts. It preserves manufacturer
reference-only limitations and unresolved physical fit/rating checks. Passive
cases01/02 explicitly prohibit adding a fan requirement; only case03 uses the
uploaded synthetic fan curve. No standards/thermal correctness claim is added.

The actual clean Wright gateway probe passed using
`wright:standard-linux-amd64-mcp-isolated`, verified local image ID
`sha256:512b001cbffd0551969630eaf618d9d4fb72987bdfb6422d9d444d8ad2e8ee73`.
Only selected renderer dependencies were added in a separate environment:
mcp1.30.0, pypdf6.9.1, pypdfium2 5.13.0 and Pillow12.3.0.
The selected image recipe is `scripts/pi_visual_runtime/Dockerfile`; the Wright
base is unchanged. Actual evidence is
`.local-run/feature-081-live/pi-visual-qualification/campaign/pi-visual-qualification/attempt-003/qualification.json`.
Ten focused source-integrity/confinement/paging/three-case-preparer tests passed.
The probe is not a campaign run and gives no campaign or content-validity credit.

The selected production image is
`sha256:c008d32758c3c3899a7af207970c174ca06a2ea261831ddc284a9d06f15c8924`.
Separate server `8ab8dee7-351d-4f2d-9a7d-fbb44163af8a` is installed, active and
enabled only for the demo by default. Its exact Docker-exec transport also passed
the actual native GatewayService probe in
`demo/campaign/pi-visual-qualification/production-001/qualification.json`.
The public scoped summary is
`docs/mcp-catalog/evidence/feature-081-pi-visual-runtime.json`.
Following deployment of the generic image and scoped-file inspection changes,
all three fresh attempt-003 instances are enrolled without dispatch. Their
manifest is `.local-run/feature-081-live/campaign-execution/pi-visual-attempt-003.json`.
Each retains nine stages and thirteen exact current tool pins. The read-only
audit verifies all original stage identities, output roles, and 15 / 16 / 17
staged input hashes; maximum per-node artifact counts are 14 / 16 / 16.
The ordinary `prepare_authorized_workflow_run` also passed for all three with
the real DB catalog and workspace document provider, and a lifecycle rejecting
every tool dispatch. Evidence is
`campaign-execution/pi-visual-attempt-003-normal-preparation-verification.json`.
No engineering workflow or native CAD/CFD execution was started by this setup.

The later actual case01 attempt003 successfully retrieved and observed primary
images, generated its complete design basis, and passed the local design review.
CAD authoring then reread 32 KiB of the same design already included in its
50,164-character connected prompt. The full observation plus its textual
representation exceeded the model transport policy; no CAD source or native CAD
mutation occurred. The complete failed run remains preserved.

Future attempt004 retains the full connected design and fixed contract, requests
only actual file hashes/byte counts for those already-present documents, and
pages other needed text at 4096 bytes until complete. It does not truncate design
or input evidence. The CAD contract inspection allows thirty-two calls to read
complete paged contracts and actual binary metadata. Three preparer regression
cases and Ruff passed.

All three fresh attempt004 cases are enrolled in
`.local-run/feature-081-live/campaign-execution/pi-visual-attempt-004.json`.
Their immutable input-binding sidecars validate completely against saved sources
and original input hashes. Ordinary preparation passed for all three with a
no-dispatch lifecycle, and the read-only audit reports three passes, no warnings
and no failures. Proof files are
`campaign-execution/pi-visual-attempt-004-normal-preparation-verification.json`
and `campaign-execution/pi-visual-attempt004-readonly-audit.json`. All earlier
attempts remain unchanged; setup dispatched no engineering workflow.

Actual case02 attempt005 later completed research, the design basis and local
review, then requested `inputs/design-basis.md` instead of the generated file
under `artifacts`. Its failed read and completed basis remain in the original
run; no CAD mutation occurred. Future attempt006 now names both exact metadata
inspection paths: the compiled design-basis output and the uploaded fixed CFD
contract. Complete connected text, 4096-byte paging, visual observations and
AgentCAD entrypoint guidance are retained.

All three fresh attempt006 cases are enrolled in
`.local-run/feature-081-live/campaign-execution/pi-visual-attempt-006.json`.
Ordinary preparation passed for all three against the real catalog and grants
with a lifecycle that rejects every tool dispatch. The proof
`campaign-execution/pi-visual-attempt-006-normal-preparation-verification.json`
checks the inspection paths against the actual compiled producer, hash-verifies
the staged contract and all inputs, verifies input-binding sidecars, retains all
original IDs, and confirms nine stages and thirteen current tool pins per case.
Only three private empty-project initialization prerequisites ran. No CAD/CFD
workflow ran, and attempt005 source digests remain unchanged. The corresponding
three-case preparer regression checks actual staged bytes and graph paths;
all three cases and Ruff pass.

Fresh attempt008 incorporates the selected build123d rotation signature and
direct literal `show_object(actual_shape)` guidance through the shared source
contract. All three cases are enrolled in
`campaign-execution/pi-visual-attempt-008.json`, with staging hashes in
`campaign-execution/pi008-staging-verification.json`. Their ordinary preparation
passed with zero tool dispatches: original task IDs and edges, nine stages,
thirteen current tool pins per case, exact input maps/hashes, both metadata
inspection paths, complete source guidance, and absent declared outputs/run
directories were verified. Proof is
`campaign-execution/pi-visual-attempt-008-normal-preparation-verification.json`.
Only the three isolated empty-project initialization prerequisites ran.
The before/after authority audit confirms eighteen prior grants and 310 prior
files (sources, inputs, combined manifests and durable Pi run records) unchanged
in `campaign-execution/pi008-prior-authority-verification.json`. These new
instances have not executed CAD, CFD or any engineering workflow.

All three fresh attempt009 cases are now enrolled with the shared Axis rotation,
Align enum and direct literal capture guidance. The immutable replacement
manifest is `campaign-execution/pi-visual-attempt-009.json`; staging identity and
initialization evidence is `campaign-execution/pi009-staging-verification.json`.
Ordinary no-dispatch preparation passed for all three with nine stages,
thirteen current tool pins each and 15/16/17 exact input hashes/maps. Original
IDs/edges, both exact inspection paths and the complete shared guidance were
verified; all declared outputs and run directories were absent. Evidence is
`campaign-execution/pi-visual-attempt-009-normal-preparation-verification.json`.
The preservation check confirmed 21 prior grants and 542 prior files unchanged,
including prior engineering artifacts and durable runs, in
`campaign-execution/pi009-prior-authority-verification.json`. Calls were limited
to normal template/save/enrollment management and three isolated empty-project
`agentcad init` prerequisites; no MCP tool, CAD/CFD or workflow run was dispatched.
