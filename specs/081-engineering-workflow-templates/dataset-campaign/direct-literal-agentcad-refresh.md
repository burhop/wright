# Direct literal AgentCAD capture guidance: fresh preparations

Prepared 2026-09-12 after a real heat attempt authored an alias for the injected `show_object` callback. The selected AgentCAD validator requires a literal `show_object(actual_shape)` call. The shared source contract now explains both the actual `__agentcad_script__` entrypoint and this literal callback requirement; aliases, replacement callbacks and silently skipped source bodies remain prohibited.

Twelve new API-created template instances were bound, saved and enrolled without dispatch:

| Family | Fresh attempt | Cases | Stages per case |
| --- | --- | --- | --- |
| Lightweight equipment bracket | 005 | 3 | 12 |
| Parametric drill jig | 008 | 3 | 8 |
| Heat spreader sizing | 008 | 3 | 9 |
| Raspberry Pi enclosure with visual references | 007 | 3 | 9 |

Each source-authoring prompt, including the independent jig-inspection author, contains the exact current shared guidance. The original template stages and edges, circle-center guidance, native FE operation, and Pi visual/context/metadata-path fixes remain present. Input mappings and hashes match the staged original packs and current dataset fingerprints. All allowed tool pins match the actual 214-tool API catalog; the selected eleven-tool AgentCAD aggregate remains `3a143c486a147380eea0d5e1f82bae394e0437c41dd5e2a7388c8ca09919ae54`.

Every declared generated file and saved workflow report is absent before dispatch. Each fresh attempt has its own successful private AgentCAD initialization prerequisite; artifact directories therefore contain only the recorded initialization/project files, not engineering outputs. No model, CAD generation, native inspection, CFD, solver or workflow run was dispatched. No KiCad container or API lifecycle change was made.

All twelve cases passed `prepare_authorized_workflow_run`, the ordinary API preparation path, with a lifecycle adapter that raises on any attempted tool start or call. The source-contract, CAD observation preparer, Pi visual preparer and input-binding tests passed: **31 tests**. Source, input, execution-manifest and immutable grant snapshots for **58 prior attempts** were verified unchanged.

Immutable local execution manifests under `.local-run/feature-081-live/campaign-execution/`:

- `direct-literal-twelve-replacements.json` — all twelve cases.
- `bracket-attempt-005.json`, `jig-attempt-008.json`, `heat-attempt-008.json`, `pi-visual-attempt-007.json` — separate families.
- `direct-literal-twelve-staging-verification.json` — actual current source/grant/input/pin checks and initialization-only inventories.
- `direct-literal-twelve-normal-preparation-verification.json` — twelve successful ordinary preparations and zero tool calls.
- `direct-literal-twelve-prior-authority.json` and `direct-literal-twelve-prior-preservation.json` — earlier-attempt preservation evidence.

These are preparation results. They do not establish full workflow completion or engineering-output correctness. The root campaign runner chooses when to dispatch these fresh immutable attempts.
