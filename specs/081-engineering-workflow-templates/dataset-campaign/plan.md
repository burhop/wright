# Feature 081 testing amendment: thirty engineering input datasets

Requested 2026-09-12. Remains on codex/081-engineering-workflow-templates and
extends the current Spec Kit testing cycle. Preserve the ten existing template
identities. This is engineering integration work, not a usability study.

**Approved recovery amendment (September 12, recorded September 13 UTC):**
Follow [focused-recovery.md](focused-recovery.md) and
[native-application-lifecycle.md](contracts/native-application-lifecycle.md).
Use exact failure probes, one full pilot per family before sibling expansion,
bounded canonical repair, compact/direct tool stages, and resource-aware queues.
Manage Solid Edge/Blender ownership, reuse and shutdown explicitly. These
documents supersede the broad flagship-first batch sequence below where it
would delay independent ready cases. The completion target remains 30/30/30/0.

## Scope and authority

Prepare three different fictional engineering briefs per template, human input
files, a reproducible campaign runner, manual/automatic test approvals, output
directories, and a live historical dashboard. Measure file presence only in this
cycle. Do not change engineering qualification based on presence checks.

The user explicitly requested setup and an unattended /goal without routine
stops. This authorizes local dataset/test infrastructure and the goal's ordinary
plan/task/implementation/test checkpoints for this bounded campaign. It
supersedes the parent plan's routine review pauses for this campaign only.
Preserve device/supplier controls, identity, RBAC, integrity and unknown-outcome
protection. No constitution amendment or production approval is implied.
The supplied goal is reviewable; its existence does not start execution.

## Research and architecture

See [runtime-findings.md](runtime-findings.md) for exact code/API evidence.
Installed servers, UI checks and the sliced fixture do not establish runnable
chains. Readiness projection, missing operation bindings, input wiring and
incomplete resume behavior are prerequisites to actual execution. Implement
these generically through existing services; never add template-ID dispatch
or bypass the readiness gate.

Separate integration execution-readiness from engineering qualification.
The test profile may execute when declared bindings, typed inputs, required
hosts/credentials, isolation and approval policy are ready. It must not require
future content-correctness qualification or overwrite public ready/verified
status. Implement an explicit scoped readiness contract with real evidence;
do not set a global ready flag to get past the current router.

Reuse the canonical .workflow.wflow authority, normal API execution,
GatewayService and useful parts of workflow_campaign.py. The new
engineering_dataset_campaign.py is development-test inventory/evidence
infrastructure, not a competing executor or a new production API.

Original vector concepts have raster upload companions. Fictional company
specifications carry explicit units and assumptions. Manufacturer links that
have not been retrieved are research tasks, not established facts. Do not copy
private customer data, standards text, stock photography or secrets.

## Input, run and output contracts

Inputs live in tests/datasets/engineering-workflows/scenarios. Each manifest
declares a profile, prompt, context documents/tables, images, provenance, output
roles/patterns and eventual canonical input bindings. Complete means every
declared file exists and is nonempty. Manifest plus file hashes form the input
digest. Enforce three unique IDs per template, thirty total.

Snapshot instantiated source/digest, packaged template revision, input bytes,
operation/server/tool/schema identities, model, credential references, campaign
policy and start/end times. Credentials never appear in assets/receipts/logs.
Input normalization, such as CSV to ROS bag, is a recorded operation that keeps
the human-uploaded originals. Empty input_bindings is not execution readiness.

Actual outputs live in artifacts/engineering-workflow-datasets/output/
scenario-id/attempt-id, with run.json, artifacts/, logs/ and approvals/.
Every produced file entry includes runtime artifact_id, relative path and
sha256. Copy only this attempt's generated artifacts. Inputs, existing files,
logs, screenshots and summaries cannot replace declared engineering outputs.
Check nonempty files, exact identities and expected roles; semantic correctness,
solver validity and compliance remain outside this testing cycle.

SQLite WAL stores registered revisions, attempts, issues and append-only UTC
events. status.json is an atomic read-only projection. The real zero baseline
is never backdated. Persist changes before display and preserve history on
restart. Show whether completion belongs to the current dataset/template revision.

## Approval and external effects

Manual and auto are explicit campaign configuration. Manual uses existing user
decisions. Auto uses an attributed integration-test actor, exact subject digest
and test policy through the same decision/resume mechanism. Never impersonate a
human or add a workspace-wide approval bypass. Keep identity, idempotency,
expiry, restart, cancellation and unknown-dispatch safeguards.

Auto covers local review gates. Actual engineering tools generate results.
Printer/supplier/purchase/publication effects use explicitly bound test
destinations selected before checkpoint creation. Auto covers only those test
destinations. Unknown actions/destinations block the case while others continue.
Prove negative tests reject real destinations under test-auto authority.
Actual CAD/solver jobs may use an explicitly configured disposable engineering
test project and authorized provider access; do not simulate an engineering
operation merely because the provider records a job in an account. Do not buy
access or start an unapproved paid service.

A full integration run executes every canonical step with real engineering
operations and declared simulated external handoffs. It may count as output
complete, with scope labeled integration, but never establishes live printer
or supplier qualification. Whole-chain fixture playback, substitute CAD/solver
outputs and truncated/local-phase-only definitions cannot count as completion.

## Metrics and dashboard

All four cumulative unique scenario/template counts have a 0–30 axis:

1. datasets_created: complete registered human input packs.
2. combinations_run: the runtime accepted execution and persisted run_started.
   HTTP/preflight rejection does not count.
3. processes_with_outputs: every required canonical step succeeded, all gates
   resolved and every expected role exists. Early completed projection alone
   is insufficient.
4. valid_data: always zero, visibly Not measured.

Retries never inflate counts. Historical achievements stay in history, while
current-revision completion, attempts and blocked/failed reasons remain separate.
Plot all four series against observed UTC time, including the flat zero line.
No manufactured progress or planned dates.

## Delivery and recovery

Prepare packs, observer, dashboard and prompt first. The goal then updates
Spec Kit tasks/analyze, fixes generic input/binding/resume gaps, connects the
actual executor and runs all thirty pairs. Complete reusable real chains in the
amended recovery order: prioritize ready flagship pilots, prove one complete
pilot per family, then expand its siblings. Continue ready independent follow-ons
while a flagship resource is blocked. Keep all thirty cases in scope.

Serialize shared CAD/MCP hosts initially; parallelize independent research,
input preparation and isolated tests. Allow at most two independent execution
lanes after proving native resource isolation. Use exclusive application leases,
bounded startup/idle/close/quit policies, and ownership-checked cleanup on every
terminal/restart path. Preserve user-opened applications and unrelated work.
Use operation/case timeouts, at most two safe local corrections per failed stage,
and family quarantine until a material fix passes its failed probe. Preserve
every attempt and never replay unknown mutations. Missing access/licenses or
ambiguous cleanup become explicit resource blockers; continue unaffected work.

Track actual model identity and token/duration observations where available.
Prefer direct generic operations for fixed calls and relevant compact context
for model decisions. The Sol goal in [goal.md](goal.md) applies these efficiency
rules without weakening engineering fidelity or inventing cost figures.

Checkpoint in execution-state.md after attempts and context boundaries with
restart commands, process identities, next steps and blockers. Do not reset
history or spin on identical failures. Completion requires 30 inputs, 30
accepted pairs, 30 full integration processes with expected files and validity 0.
If only external blockers remain, retain resumable state and report them.
Completion also requires native lifecycle reconciliation and final owned-session
cleanup receipts; the four counters retain their original definitions.

## Constitution and verification

Keep product runtime changes in their existing domain/application/storage
boundaries. Test infrastructure is offline, uses SQLite/files and no cloud
dashboard or new database service. Selected MCP software remains outside the
base appliance; clean-container qualification remains a distinct process.
Maintain canonical authoring, path confinement, credential references and
structured tracing. Follow workflow-ui-integration.md and applicable three-tier
tests if changing the workflow-facing UI.

No push, merge, release or publication is authorized by this local campaign.
Preserve unrelated dirty work. User-directed unattended continuation applies
to routine local milestones, not production authority.

Verify 10×3 corpus completeness, original provenance, raster companions, unique
counters across retries/restarts, revision identity, no fixture/preflight
inflation, missing/empty/out-of-directory artifacts, zero validity, manual/auto
configuration, same-origin writes, responsive chart/file links, and actual
canonical execution through two sequential approvals to a true terminal step.
