# Quickstart

Enable bridge feature, invoke a declared read-only tool, then a mutating one
without approval; verify the second is denied. Revoke approval and retry;
verify cross-workspace and direct-MCP attempts fail.
