# React Flow Recovery Evidence

**Dependency**: `@xyflow/react` 12.11.3, exact version

**Scope**: feature-gated workspace **Workflows** surface opened from `/workspace/<real-id>?workflow=canonical`; no global recovery route or navigation entry
**Adapter**: retained `DraftCanvasRenderer` / `DraftCanvasAdapterProps`

## Authority boundary

React Flow receives an immutable `DraftProjection` plus selection and an intent callback. A separate context supplies run, proposal, artifact, and non-data relationship overlays. It does not assign semantic IDs, validate, serialize authority, apply commands, accept proposals, or own run facts.

## Prior exact-subject evidence

- nine-step mounting-bracket graph without decorative phase lanes;
- typed visible handles and separate artifact actions;
- pointer connection by source/target handle coordinates;
- keyboard handle contract using Enter/Space and Escape cancellation;
- live drag motion followed by a layout-only commit on release;
- accessible edge selection, disconnect, and stable relationship identity;
- data, decision, and feedback relationship treatments;
- queued/running/needs-input/blocked/failed/stale/succeeded node states;
- active block banner and active edge text/pattern/arrow cues;
- reduced-motion active edge retains a static pattern and `Active flow` label;
- minimap, zoom controls, and fit-view mechanics;
- 390×844 document containment;
- zero serious/critical axe violations in the base concept and port laboratory.

The historical direction-approval package is the [passing walkthrough](../../../artifacts/ui-walkthrough/workflow-recovery/20260901T031913Z-continuation-1/report.html) for commit `f9237763d6fa6e9748dfb7b713e753a7fc4b4d17`, tree `aeca6ab8294dd54112d3e9ac10148537af32b0f0`, and manifest SHA-256 `f2b4964ec1f599b55a8a8d53704147d2133674baa5db9a28072d4a2808c57347`. It contains 50 passing steps, 99 raw and 99 annotated screenshots, a 94,777,058-byte trace, and zero browser diagnostics. [Product approval](product-approval.md) remains bound to that exact subject.

The later `c5fb7d8e4a722f84956ebe22085e7fdf38b1b1d5` / continuation-5 package is prior automated correction evidence: 12/12 steps at 1070×791, 12 raw and 12 annotated screenshots, a 16,542,037-byte trace, zero browser diagnostics, tree `148a935edd38e9abe91b9ed284cd04882acf59c6`, and manifest SHA-256 `e661bf45ff1449abc87399b928156fc336f367f260368a2966645a0026afd96e`. It does not constitute a second human approval.

## Workspace-owned correction awaiting exact evidence

The current correction changes the surrounding product contract while retaining React Flow only as a renderer:

- entry is workspace-owned through **Workflows**, never a global recovery destination; that action idempotently bootstraps and immediately opens the validated default when absent, while re-entry preserves an existing source unchanged;
- Diagram, Source, and Side by side project one visible `.workflow.wflow` source file;
- groups are optional and omitted from the short mounting-bracket workflow when they do not add meaning;
- step search is absent through 25 steps and enabled at 26 or more;
- the 1070×791 desktop shell is fixed-height with no document/page scrolling;
- host revision, digest, compare-and-swap, and integrity fields stay outside engineer-authored source and appear only in technical details;
- design-intent attachment state, simulation state, and demo STEP/report outputs are not claimed as workspace-persisted workflow content.

A fresh exact commit, tree, raw/annotated walkthrough, trace, diagnostics, manifest, and validated report are explicitly pending. Until that package exists, these changes are implementation and automated-test claims rather than new exact-subject walkthrough evidence.

## Test evidence

`tests/ui-integration/workflow-recovery.spec.ts` uses real Chromium and real React Flow behind a mocked workspace/API boundary. It exercises workspace entry, automatic first-entry bootstrap/open, unchanged re-entry, direct connection, revision isolation, paired source editing, invalid containment, AI reject/accept, needs-input recovery, demo output lineage/download, reduced motion, automated accessibility, and fixed-height containment. Backend and frontend focused tests separately cover the visible workspace source file, side-effect-free read, idempotent create-if-absent, existing-source preservation, compare-and-swap save, stale-save containment, and the absence of global routes. A browser test's demo attachment or download does not prove durable attachment/output persistence.

The frozen prototype’s 91/100 bakeoff and single 100-node run remain supporting but non-qualifying evidence. Current bounded behavior keeps the detailed canvas and omits search through 25 steps, then enables deterministic compact navigation and search at 26 or more. This recovery slice does not promote a permanent renderer or claim production-scale performance.
